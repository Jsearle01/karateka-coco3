## Form B Report — Recon 1: scene-6 scroll-stop + bounded-scene measurement

### §1  Timing (C-35)
t0=2026-07-20T19:09:08.906468100Z
commit-time=<this commit, %cI> (hash in git log; stated inline in the dispatch reply)
Elapsed: ~ (recon; see inline). Pure recon — NO build, NO code change.

### §2  Summary
Traced the oracle (`apple2e`, `dumps/karateka.dsk`) attract scene-6 through the fight → walk-off →
loop-back, execution-confirming all five measurements. **The scroll (`$52`) does NOT ramp to a
right-extent and stop; it steps down with the walk cluster and halts via a phase-wrap at `$52==$1A`
(settles `$1B`). The player (`$62`) then advances alone to `$62≥$2A`, which arms PRGEND (`$AF=1`)
and the demo executes a resident `JMP $B766` loop-back — a PLAYER-POSITION trigger, not an archway
flag.** The archway/castle routine (`draw_castle_af76`) DOES draw in the attract (167×, fixed column
`$1B/$1C`), correcting a disasm comment. Ground-truth note (CLAUDE.md §2): every claim below is from
execution (write-taps / `-debug` breakpoints / blit-trampoline taps), not from labels; where a comment
conflicted with execution (the "fires 0 in attract" annotation) **execution won**.

### §3  Files added (recon instruments — harness tooling, not game code)
- `harness/tools/recon1_scroll_traj.lua` — write-tap `$52/$62/$72/$51` change-trajectory.
- `harness/tools/recon1_drawprog.lua` — blit-trampoline (`$1903/06/09/0C`) draw-program capture, walk-off window.
- `harness/tools/recon1_confirm.lua` — write-tap `$52`/`$AF`/`$62` with PC (halt PC + PRGEND arm), run ×3.
- `harness/tools/recon1_castle.lua` — `-debug` bp on `$AF76` (castle) with RAM sentinel; fire detector.
- `build/logs/recon1_*.txt` — raw trace outputs (evidence).

### §4  The five measurements (execution evidence)

**M1 — Scroll-stop condition + bound.**
`$52` (scene scroll) steps DOWN with the walk cluster `$52/$62/$72/$91` (dec-batch `$B357-$B35D`,
`dec $52` at pc=`$B35B`). The HALT is the phase-wrap in `routine_b30f`: when `$52` reaches **`$1A`**
(`cmp #$1A`, ~`$B363`) the phase-advances (`inc $52`→**`$1B`** at pc=`$B372`, and zeroes `$50`/`$53`,
decoupling the walk-step). After the wrap `$52` **pins at `$1B`** and stops changing.
Evidence (recon1_confirm, ×3 byte-identical): last `$52` writes `f=8761 val=1B pc=B35B / val=1A pc=B35B
/ val=1B pc=B372`. **Bound = `$1A` (compare); scroll settles `$1B`.**

**M2 — Archway = `$52`-relative? → NO, FIXED column; and it DOES draw in attract.**
`draw_castle_af76` (`$AF76`, dispatch slot 4) **fired 167× over f6003–f8943** (recon1_castle `-debug`
bp) — INCLUDING the walk-off (f8866–8943), right up to the transition (f8951). It draws via the
`L0A00/L0A03` pattern-fill path at **hard-coded columns `$05=$1B`/`$1C`** (disasm `$AF86+`), gated on
`$E1` (cached scroll) ∈ `[$14,$28]`. Because it uses the fill path (not the `$1903` trampolines), the
trampoline draw trace did not see it — but the bp proves it runs. **Framed position at the stop:
fixed col `$1B/$1C`, persisting while `$52=$1B` and the player walks past.** (Whether it renders a
literal arch shape vs wall/floor backdrop patterns is a VISUAL question for Jay's gate — PNG rules;
execution establishes the column SOURCE = fixed, and that it draws.)
**CORRECTION surfaced:** the disasm annotation "`draw_castle_af76` FIRES: 0 in attract (gameplay-only)"
is WRONG per execution — flag for the disasm/postmortem.

**M3 — Guard = `$52`-relative?**
Guard `$72` ends **`$0E`** (defeated/low) — execution-confirmed ×3 (recon1_confirm). Guard combatants
draw via the `$52`-relative loader (`load_scene_sprite_ae3f`: X=`$52±xadj`); guard head `$8EC1` clusters
col **`$0F–$14`** in the stable pre-transition window. The guard does NOT slide in with the walk-off —
it is defeated/parked; the sole advancing figure is the player (`$62`). (Caveat: `$8EC1`/`$8E9B` both
appear at col `$2A` in the f8916–8947 transition frames — flagged, not over-interpreted; the exact
walk-off figure identity is a Jay visual-gate question.)

**M4 — Fills: extend-rule + extent; off-screen buffer ruled OUT.**
Draw-on-demand: every visible frame RE-BLITS the scene cels at columns recomputed from `$52`
(`load_scene_sprite_ae3f`) — there is **no wide off-screen buffer + scroll-copy**. Evidence (recon1_draw):
cliff `AB8E` at col `$FD/$FE` (negative = far-left edge); midground `A684` re-blit at cols `$22→$2B`
stepping rows `$98–$AA` (a per-frame vertical fill); castle pattern-fills at fixed `$1B/$1C`. As the
cluster steps, the cels' computed columns shift and the frame is redrawn — draw-on-demand.
**Right extent ≈ col `$2B`** (the midground follows the player past the `$28` visible edge).
**Off-screen-buffer reveal: RULED OUT.**

**M5 — Loop-back + transition point.**
`routine_b260` (`$B260`, called once/iteration from `attract_main_b584` at `$B5D0 jsr LB260`) arms
**PRGEND (`$AF:=1`) at pc=`$B27D` when player `$62 ≥ $2A`** (`cmp #$2A` at `$B277`), plus `$44==0`
(input idle) and `$30==0`. The Q012 gate at `$B5C5-$B5D7` then does `LDA $AF / BEQ / JMP $B766`.
**`$B766` = jmptable_b760 slot 2 = the cycle-restart target — a RESIDENT in-attract `JMP`, not a disk
reload.** PRGEND clears at `$B440` on scene-5 re-init (re-arms next cycle).
Evidence (recon1_confirm ×3): `PRGEND f=8951 af=01 pc=B27D 62=2B 44=00 30=00`; `P62>=2A f=8951 val=2B
pc=7091` (player pos written by the walk routine at `$7091`).
**Stage B's responsibility ENDS here:** at the `$62≥$2A` PRGEND arm — Stage B builds the scroll +
walk-off up to this trigger; the loop-back JMP itself is Recon 2 / the disk-boot arc.

### §5  Independence rulings (§2 — alternative ruled out explicitly)
- **Fills:** draw-on-demand (RULED IN) vs off-screen-buffer reveal (RULED OUT) — per-frame `$52`-relative re-blit, no buffer/scroll-copy.
- **Archway:** FIXED column `$1B/$1C` (RULED IN) vs `$52`-relative (RULED OUT) — hard-coded `sta $05`.
- **Guard:** `$52`-relative via the combatant loader (RULED IN for the draw), but defeated/parked at the walk-off (does not slide with it).
- **Transition:** PLAYER-POSITION `$62≥$2A` (RULED IN) vs archway-triggered (RULED OUT) — `routine_b260` reads `$62`, never an arch flag.
- **End-of-scroll:** in-demo RESIDENT scene-advance `JMP $B766` (RULED IN) vs jmp-to-disk-reload/not-resident (RULED OUT).

### §6  Consistency check (§3)
Order (in player-`$62` terms): **scroll-stop (player `$62≈$13` at f8761) < arch fixed col `$1B/$1C`
< transition (player `$62=$2A` at f8951).** The player walks from `~$13`, THROUGH the arch column
`$1B/$1C`, to the transition `$2A`. **Jay's remembered order (stop → walk toward/through arch →
transition) HOLDS** — with the refinement that it's the PLAYER advancing across a FIXED arch/backdrop
and a STOPPED scroll, not the scroll carrying the arch to a stop.

### §7  Attract-run variance (§0)
3 fixed-boot headless runs (recon1_confirm, 185 s each) were **byte-identical**: halt `f8761 $52→$1B
pc=B372`, arm `f8951 $62=$2B pc=B27D`, transition frame f8951 — all three runs. Per idioms §3 this is
**fixed-boot seed-determinism**, NOT proof of run-to-run invariance for the stochastic fight: N
identical fixed-boot runs prove nothing about the distribution. The STRUCTURAL invariants — the compares
`$52==$1A` and `$62≥$2A`, the PCs `$B372`/`$B27D`/`$B260`, and the loop-back `JMP $B766` — are
code-constants and therefore **seed-invariant by construction**. Frame NUMBERS would shift with fight
length under seed variance. **Residue:** I did not seed-perturb (`$59` poke) this pass to exercise a
varied fight; the endpoint conditions are code-constant so they will not move, but the frame timings
between them are not bounded by these runs. Flagged, not over-claimed.

### §8  Numbers Stage B can build against
- Scroll `$52` operating band ≈ `$30`→`$1A`; **halt/phase-boundary `$52==$1A`, settles `$1B`**.
- Sprite X = `$52 + xadj[i]` (visible, `$52≥$14`) / `$52 - xadj[i]` (mirror, `$52<$14`); scene bail `$52≥$29`.
- Archway/backdrop: FIXED cols `$1B`/`$1C`, gated `$E1∈[$14,$28]`.
- Fill right extent ≈ col `$2B`; cliff left edge col `$FD/$FE`; draw-on-demand (no buffer).
- Guard final `$72=$0E`; guard head stable col `$0F–$14`.
- **Stage-B END trigger: player `$62 ≥ $2A`** (PRGEND arm at `$B27D`); loop-back `JMP $B766` (Recon 2).

### §9  Uncertainty flags / residue
- **Plan file `plan_recon1-scroll-stop.md` NOT FOUND** in the repo or the orchestrator dir — worked from the dispatch §1–§3 (self-contained). Flag.
- **Disasm comment error:** `draw_castle_af76` "fires 0 in attract" contradicted by execution (167 fires) — for the disasm/postmortem to fix.
- Walk-off figure identity (`$8E9B`/`$8EC1` co-located at col `$2A`, f8916–8947) — Jay visual-gate question, not resolved by the position trace.
- Whether `draw_castle_af76` renders a literal archway vs wall/floor backdrop — PNG/visual gate (Jay), not a Clyde pixel interpretation.
- Seed-perturbation variance not run (see §7).

### §10  25.3 (Jay MAME visual gate)
PENDING JAY. Suggested gate: run the oracle attract at half speed and observe, at the scene-6 end,
the SCROLL halt while the player walks rightward across a fixed backdrop and the demo loops back to the
title (no on-screen "walk through a moving archway"). All numeric claims above are execution-grade;
the on-screen appearance (esp. the archway shape) is Jay's to gate.

### §11  Candidate(s) captured
`2026-07-20-execution-outranks-a-disasm-fires-count-annotation` (a FIRES:n / "gameplay-only" comment
from an old/narrow trace can be wrong — re-confirm firing by execution before concluding a routine is
dead in a mode; the castle "fires 0 in attract" was 167).

### §12  Commit
<hash — stated inline>.
