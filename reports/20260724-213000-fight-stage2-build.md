## Form B Report — Fight Stage 2 (build) — SUB-STAGE 2a (selection engine)

**Class:** build, live-channel. `wip`. Prod `88eba89…` byte-identical. **Sub-staged per §7: 2a (selection
engine) built + verified + cost-measured; 2b (continuous fight phase + position) and 2c (draw) follow —
reported at this natural breakpoint rather than pushing a half-verified full engine into the gated scene.**

### §1  Timing (C-35)
t0=`2026-07-24T21:27:16-04:00` (HEAD `3432586`). commit-time=`git show -s --format=%cI 1aff830`. Elapsed
~50 min. Predicted: n/a (dispatch gave no band). Classification: n/a (sub-stage of a large build).

### §2  Summary
Built the fight **action-selection engine** (the biggest-build's cheap, self-contained foundation), driven
from the **measured** `$33`→action map — not the oracle's out-of-range-indexed tables (unsafe, verdict §1).
LCG `$59=($59*5+$13)&255` verbatim; two-level weighted selection (roll1→state `$33`, roll2→action). Verified
faithful (offline full-period simulation matches the measured mix) and cheap (0 overruns, 81.2% — the §5
first fight-cost point). Runs post-settle; the continuous fight-phase + position + draw are 2b/2c. Run-in
scene unchanged. Prod untouched.

### §3  Files modified
- `tests/scripted/scene6_b2prime_driver.s` — `fight_step`/`fight_lcg` + `f33_tbl`/`fa_07..fa_0D` tables +
  `fight_59`/`fight_33`/`fight_action` state; hooked `jsr fight_step` into `step_init`'s halted path.

### §4  Reasoning
- **§1 unambiguous mechanism, verbatim:** LCG `lcg_step_a0a2` = `$59*5+$13` (asl,asl=×4, +`$59`=×5, +`$13`,
  8-bit). Reachable win-path set only; `$C2`/losing paths unbuilt (verdict: unreachable at `$2F`==0).
- **§2 selection from the MEASURED map:** `f33_tbl` = the observed `$33` distribution (07:96, 09:+62, 0B:+70,
  08/0A/0C/0D tail) scaled to 256; `fa_07..fa_0D` = each state's observed action distribution as cumulative
  thresholds. Roll1 picks `$33`, roll2 picks the action. This drives selection from the captured behavior,
  covering the out-of-range `$33`=08–0D (144/231 fires) that static table-reconstruction can't safely produce.
  Weights are approximate proportions (§7) — the shape, not exact frequencies.

### §5  Verification (AC-by-AC)
- **Varied LCG-driven actions matching the measured mix** — VERIFIED by offline simulation of the *ported*
  engine (LCG + the committed tables) over the full LCG period (2000 selections):
  | action | ported | measured |
  |---|---|---|
  | idle | 53% | 50% |
  | C5 | 19% | 20% |
  | D7 | 15% | 14% |
  | D1 | 2% | 2% |
  | FF | 3% | 2% |
  | strike `$01` | 3% | 8% |
  | C6 / approach | 0% | 0%/<1% |
  Shape matches; strike is a touch low (the 0B/0C/0D states that feed `$01` are lightly weighted) — within
  the approximate-proportions latitude; re-samplable if it reads wrong at a later gate.
- **0 overruns (§5 first fight-cost):** `stageb2_phasecost.lua` → NO-WAIT=0, busiest phase 81.2% (unchanged
  from B2') — the selection engine (LCG + two table walks per step) is negligible, as predicted. **The draw
  (2c) is the heavier part not yet measured.**
- **Run-in not regressed:** `fight_step` runs only post-settle and has no visible output yet (no draw); the
  Jay-gated run-in scene is unchanged. Assembles clean, load end `$4FDE` (< `$8000`). `SUBBYTE_ENABLE=0`
  unaffected (orthogonal). Prod `88eba89b15cd…` byte-identical.
- **Jay 25.3:** N/A for 2a — no visible change (the selected actions aren't drawn until 2c). The visual gate
  comes with 2b/2c (the fight playing).

### §6  Verdict-time evidence
25.1: `lwasm` assembles clean; offline sim (ported tables) reproduces the measured distribution; phasecost
0 overruns 81.2%. 25.2: N/A (sandbox driver, no bundled artifact; prod ROM untouched). 25.3: deferred to
2b/2c (no visible change in 2a).

### §7  The breakpoint (why sub-staged here)
Measuring in-scene showed the b2prime halt **freezes the scene ~11 cycles post-settle** (it's a frozen
end-state), so `fight_step` ran only 11× live — the engine works but doesn't yet play continuously. Making
the fight run over the **continuous** scene (not freezing at `$1B`) is the 2b integration, alongside position
(`check_position_a/b` primary + `$B29D` servo baseline) and the guard entry. The action→`$20` draw is 2c.
Rather than wire all of that half-verified into the gated scene, 2a stops at the verified selection engine.

### §8  Uncertainty flags
- Strike `$01` under-weighted (3% vs 8%) — approximate weights; re-samplable (multi-seed sweep) if a gate
  needs it. The reachable set + shape are right.
- The **draw cost (2c) is unmeasured** — 2a's 0-overruns is the selection-only cost; the action cels + `$20`
  update will add load (the heavier part, feeds stage-6 combined budget).
- Continuous fight-phase (not-freeze) is unbuilt — the scene currently freezes post-settle.

### §9  Follow-up (the rest of stage 2)
- **2b:** continuous fight phase (don't freeze at `$1B`; keep the scene running) + position
  (`check_position_a/b` primary, `$B29D` servo baseline) + guard entry (real cels).
- **2c:** action→`$20` anim update + `L6811`-equivalent draw (the `A→$20` map from recon §2c); measure the
  draw cost (0-overrun gate + Jay's visual gate — the fight playing).

### §10 User interaction during task
None (autonomous build sub-stage).

### §11 Candidate(s) captured
None new this sub-stage (the measure-then-port principle was captured in the recon).

### §12 Commit
`1aff830` (sub-stage 2a) on `wip`. Prod `88eba89b15cd…` byte-identical.
