## Report — Fight Stage 1: servo/positioning composition (verified) + port spec

**Class:** build stage 1. `wip`. Prod `88eba89…` byte-identical. **Status: §2 composition CONFIRMED from
execution (the dispatch's load-bearing "verify, don't assume") — and it materially refines the framing;
servo port spec'd from the code; port build is the informed next step (see §4).** No driver change yet.

### §0  Receipt / status
t0=`2026-07-24T15:18:25-04:00` (HEAD `6de93b0`, `wip`). git status clean (pre-existing untracked only).
Prod `build/karateka.bin` sha1 `88eba89b15cd…` untouched.

### §1  The servo mechanism — exact spec from the oracle (`attract_state.s`)
Core routing at `L_B29B` (`$B29B`):
```
lda $62 ; cmp #$0F
beq  -> $53:=0, rts        ; $62 == $0F (fighting distance): IDLE, clear direction/accel
bcc  -> jmp routine_b381   ; $62 <  $0F: FORWARD step
else -> jmp routine_b30f   ; $62 >  $0F: BACKWARD step
```
Plus a `$52`-phase gate above it: at `$52==$1B` (settle) or `$52==$0B` with sub-byte `$50==0`, the servo
only steps while `$62` is on the wrong side of `$0F`, else idles (`L_B2AE`/`L_B2B7`).
**Step primitives (the key coupling):**
- `routine_b381` (forward): `inc $62 ; inc $72 ; inc $52 ; inc $91` — **all four in lockstep** (on `$51`
  column-wrap forward, CMP #$28 / reset $F8).
- `routine_b30f` (backward): `dec $72 ; dec $62 ; dec $52 ; dec $91` — lockstep (on `$51` wrap back).
- `routine_b3df` (±26 jump): moves `$62/$72/$91`, **`$52` excluded** — teleport, not a step.
So `$52` (scroll) and `$62` (player pos) are the **same servo motion** at single-step; only the ±26 jump
decouples them. The scripted `$52` sweep was scaffolding for exactly this.

### §2  Composition — CONFIRMED from the trace (change-point trajectory, f6000–9200, seed-deterministic)
Tool: `harness/tools/oracle_fight_compo.lua` (logs `$52/$62/$72/$53/$59/$33` at every change). **The scene is
THREE phases, not "scroll + concurrent fight":**

| Phase | frames | `$52` (scroll) | `$62` (player) | `$72` (guard) | notes |
|---|---|---|---|---|---|
| **WALK-IN** | 6000–6448 | **30 (FIXED)** | **0B→0F** | 30 (fixed) | player walks to fighting distance; **NO scroll**, arch off-screen |
| **FIGHT** | 6455–~8630 | **30→1B** (oscillates) | **holds ~0F** | 30→0E (approaches + fights) | scroll+guard+combat CONCURRENT; `$59` fight-seed active from f6699 |
| **TRAVERSE** | ~8668+ | 1B (settled) | **0F→2A+** | 0E (guard down) | guard defeated (`$33` collapses) → player traverses to `$2A` (B3 loop-close) |

Answers to the three §2 questions:
- **Q1 — servo vs scroll:** NOT concurrent from the start. **The walk-in (`$62`→`$0F`) runs with `$52`
  FIXED (no scroll).** The scroll (`$52` 30→1B) happens **during the FIGHT**, in lockstep with `$72` (guard)
  and the servo'd `$62` — "during the scroll, both directions" is the fight's push/pull (`$52/$62/$72` all
  oscillate). At `$62==$0F` the `$53` flip (`$FE`, f6455) is the walk-complete handoff that starts the
  scroll+fight.
- **Q2 — `$0F` vs `$2A`:** ordered exactly as suspected — **walk-in→`$0F` → hold `$0F` through the fight →
  (guard defeated) → traverse to `$2A`.** `$62` holds `$0F` for the whole fight, then climbs `0F→2A+` only
  after `$72` drops (guard down) at f8668+.
- **Q3 — full `$62` trajectory:** `0B→0F` (walk-in) → oscillate `~0F` (fight, min 0D/max 13) → `0F→2A+`
  (post-fight traverse). `$52`: 30 fixed (walk-in) → 30→1B oscillating (fight) → 1B settled (traverse).

**Framing refinement (carry into the build):** our **B2' `$52` 30→1B scroll = the FIGHT-phase scroll**, not
the walk-in — the arch enters *during the fight*. The player **RUN** we animated is the **WALK-IN** (a
distinct earlier phase with `$52` fixed), and during the fight-scroll the player **holds `$0F` and FIGHTS in
place** (not running). B2' compressed walk-in + fight-scroll into one sweep; the real arc separates them.
The `$52`=`$1B` we settle at is the fight/traverse settle — consistent.

### §3  Acceptance status
- Servo spec (route `$62` vs `$0F`, fwd/back/idle, `$52/$62/$72` lockstep, `$53` flip at `$0F`) — **captured
  from the code (§1).**
- Composition (`$62`/`$52` co-advance, full `$62` trajectory, `$0F`→`$2A` order, guard-entry handoff at
  `$62==$0F` where `$53` flips) — **confirmed from the trace (§2).**
- The guard-entry handoff signal is the **`$53` flip at `$62==$0F`** (f6455), matching
  `walk-guard-trigger-finding.md`'s guard-head-draws-at-`$0F`. Established.
- Jay 25.3 (player walks to fighting distance, guard appears) + the servo port into the driver — **the build
  step, now correctly specified by §2** (see §4). Prod untouched; no overrun risk (servo is a few compares).

### §4  Port plan (composition-informed) — the build step
The §2 finding changes the port structure from the dispatch's "concurrent from the start":
1. **Add a `$62` player-position state + the `$B29D` servo** (route vs `$0F`; fwd/back/idle per §1). The
   step primitive advances `$62` and, **during the fight phase only**, `$52` in lockstep (walk-in keeps
   `$52` fixed).
2. **Sequence the driver as WALK-IN → FIGHT → TRAVERSE** (per §2), not one sweep: walk-in servos `$62`
   0B→0F with `$52`=30 (arch off-screen, run animation); at `$62==$0F` flip `$53` (handoff) and begin the
   fight-phase `$52` scroll (arch enters) with `$62` held at `$0F` (fight-in-place); traverse (`$62`→`$2A`)
   is B3.
3. **Retire the scripted `$52` sweep** for the fight path; the servo drives it (coupled during the fight).
This is a phase-1 restructure of the driver — real code, but now unambiguous. Recommend building it against
this confirmed composition rather than the pre-trace framing.

### §5  Out of scope
Combat state machine / hit-detection / choreography / clip-cull / combined-budget = stages 2–6. Traverse to
`$2A` = B3. No driver change this report. Prod `88eba89…` byte-identical.

### §6  Uncertainty flags
- Labels provisional past scene 4 (`$62`=player, `$72`=guard, `$59`=fight seed) — but the causal trajectory
  is execution-confirmed regardless of label (matches `walk-guard-trigger-finding.md`).
- The port (§4) is specified but unbuilt; it restructures the driver's phase-1 and should be built+gated with
  its own 0-overrun + Jay gate.

### §7  Candidate captured
Candidate-worthy: *"confirm the phase SEQUENCE from the trajectory before porting a servo — the framing said
'fight concurrent with scroll'; the trace showed walk-in (no scroll) → fight (scroll) → traverse, three
phases, which changes what the port must sequence. The doc heads compress; the trajectory separates."*

### §8  Files
- `harness/tools/oracle_fight_compo.lua` (new — the composition trace).
- `reports/20260724-152500-fight-stage1-servo.md` (this).
