# Calibration log

Per methodology v0.4 §3.5.1 timing block and Appendix D. Captures predicted-bucket vs actual-Clyde-time for each task. Bands evolve as data accumulates; refinements get noted inline.

P0 timing data lives in `project-state.md` § "Final P0 calibration summary" and Appendix D of `claude-orchestrated-methodology-v0_4.md`. This log starts at P1.

## P3 (P3-prime / OAuth + autonomy)

> **⚠️ MEASUREMENT/LOGGING GAP — T-P3-002R through T-P3-005 (no data, honest hole).**
> These five P3 tasks have **no calibration-log rows**. Per CB-CALIBRATION-AUDIT (2026-06-03): from the v0.6-codification task (T-P2-014) onward, executor reports shifted to estimate-grade C-35 blocks, and after the P2 gate close (T-P2-015) the per-task **log-append step silently dropped out of dispatches** — so even the reported numbers never reached this log. The numbers cannot be honestly reconstructed (reconstructing from commit timestamps would be the exact estimate-as-measurement failure being repaired), so the gap is **left as documented empty space, not back-filled**. Measurement + logging are restored as a standing executor step (methodology §3.4) starting with CB-CALIBRATION-REPAIR below.

| Task | Shape | Predicted band | Actual (measured) | C-14 class | Notes |
|---|---|---|---|---|---|
| CB-CALIBRATION-REPAIR | methodology-repair + pool-capture | (none stated in dispatch; analog: doc-edit + pool-capture ~10-20 min) | **≈5 min — MEASURED** (20:18:20 dispatch-receipt → ~20:23 commit-push; 4.5 min to work-complete) | sub-band-low (vs ~10-20 analog) | First restored P3-era datapoint — real clock reading (not an estimate), proving the restored pipeline end-to-end. Small surgical scope: methodology §3.4 standing log-append step + this gap-marker + 2 pool candidates (M-N correction + degradation lesson). Docs/pool only; no source/test. Sub-band because the edits were few + targeted and the analog band assumed more surface. |
| T-P3-006 | canUseTool floor extension (daemon, security-critical, +Write/Edit inspection) | (none stated; analog: new daemon module + adversarial tests ~10-20 min) | **≈11 min — MEASURED** (21:23:23 dispatch-receipt → ~21:34 commit-push) | in-band (vs ~10-20 analog) | Hard-deny floor: new `floor.ts` (~230 lines, pure path/command analysis) + `canUseTool` rewire (Bash + Write/Edit) + 19 adversarial tests + 1 SMOKE message update. Landed mid-analog — the module + thorough adversarial test matrix (evasion coverage) consumed the time; one reactive fix (a "not-covered" test was accidentally caught → corrected to a genuine gap) + a 3-error lint sweep (regex escapes + unused param). Daemon 538→559 (+21). No mid-run-escalation channel built (discipline → pool). |
| T-P3-007 | daemon event-stream + taps reusing existing infra | (none stated; analog: event stream + multi-site taps + tests ~15-25 min) | **≈17 min — MEASURED** (19:09:14 dispatch-receipt → ~19:26 commit-push) | in-band (lower-half vs ~15-25 analog) | Interaction event log: generalized `AuditLog<E>` (basename-derived archive prefix, no audit.jsonl regression) + new `interaction.ts` (event union + recorder) + taps at 3 single points (delegate handler dispatch+gate; runner 3 terminal branches; canUseTool floor_denied+push_observed) + main wiring/shutdown + 9 tests (interaction 7 + delegate 2). 2-error lint sweep (useless initializer + unnecessary cast). Daemon 559→568 (+9). Additive-optional-params recurred a 6th time (recorder optional on DelegateDeps + SdkJobRunner) — flagged, not re-captured. |
| T-P3-008 | acceptance harness + Windows-floor parity re-check + flake confirmation (gate-close automated portion) | (none stated; analog: harness assembly + parity re-check ~20-35 min, cf. T-P2-011 harness 25 min) | **≈21 min — MEASURED** (epoch 1780750204 dispatch-receipt → 1780751448; 1244s = 20.7 min) | in-band-low (vs ~20-35 analog) | New vitest acceptance suite `tests/acceptance/p3-acceptance.test.ts` (10 tests: 8 pass + 2 operator-smoke-deferred `it.skip` for AC-P3-12 live-claude.ai + AC-12e re-bind — explicitly NOT faked/stubbed) over the P3 ACs, additive (existing per-task tests retained as the per-AC coverage; the genuine new surface is the end-to-end isolation chain wiring TokenStore.mint→authenticate→WorkspaceBinding→real `get_open_editors`: A acts on A, A⊥B & B⊥A both 403, AC-11 legible "bound to workspace 'ws-a'" 403, AC-12d revoke→`invalid_token` not unconstrained). **AC-P3-13 PARITY FINDING + FIX (the load-bearing result):** the T-P3-006 floor compared paths case-SENSITIVELY (`startsWith`), but the Windows FS is case-INSENSITIVE — a case-variant of the auth-dir path (`c:\users\jay\.claude-bridge` vs `C:\Users\jay\…`) **slipped the ~/.claude-bridge self-protection** (a silent UNDER-block = real isolation hole, confirmed by a throwaway probe on this win32 host: variant write was *allowed*). This corrects my earlier (T-P3-006-era) assumption that case-variance was a safe over-denial — true for the rm-outside-workspace guard, FALSE for the auth-dir guard. Fixed by case-folding ALL floor path comparisons on win32 via a `canon()` helper (isUnder + workspace-root equality + `.git` basename + config-dir command checks) — necessarily holistic: case-folding `isUnder` alone would have *regressed* a case-variant `rm -rf <root>` from denied→allowed (it had been incidentally denied via the case-sensitive "outside workspace" fall-through). +2 platform-aware regression tests (acceptance suite + `floor.test.ts`). **bcrypt-contention flake: did NOT reproduce** across 3 consecutive full parallel `npm test --workspaces` runs — no mitigation applied (lowering bcrypt cost would weaken the security test for no demonstrated benefit; left as a watch-item). eslint `maximumDefaultProjectFileMatchCount` 50→64 (the new acceptance file tipped the daemon past 50). Daemon 568→577 (+9: 8 acceptance-suite passing incl. the in-suite parity regression + 1 `floor.test` parity regression); full suite 913 pass + 17 skip; build + lint clean (C-25.1 fresh). Live smoke (AC-P3-12) is operator-run, not in this row. |
| CB-LINUX-LAUNCH-TESTS | platform-mocked test coverage + behavior-preserving testability refactor | (none stated; analog: targeted test-add + small refactor ~10-20 min) | **≈8 min — MEASURED** (epoch 1780752858 dispatch-receipt → 1780753358; 500s = 8.3 min) | sub-band-low (vs ~10-20 analog) | Locks in the Linux-native launch/connect DECISION logic (per CB-LAUNCH-PATH-RECON: config-B correct-by-construction, but the POSIX arm had ZERO coverage). **Behavior-preserving testability refactor** (NOT a fix — recon confirmed no bug): `discoverDaemonEndpoint(platform = process.platform)` + `CLI_CANDIDATES` const → `candidatesFor(platform = process.platform)` + `platform?` injected into `LocateCliDeps`/`StartDaemonDeps` (all defaults = prior behavior; chose injection over global-`process.platform` mutation to match the package's cross-platform-test-inputs convention). **Tests:** new `discover-endpoint.test.ts` (7: win32→named-pipe-no-config-read; linux→config `ipc_socket`; linux→`daemon.sock` fallback on absent/empty/malformed; darwin generic-POSIX; default-arg behavior-preserving) + `daemon-lifecycle.test.ts` spawn-contract block (5: `candidatesFor` both arms; `locateCliBinary` linux shell:false / win32 shell:true; `startDaemon` linux bare-binary+shell:false+detached / win32 .cmd+shell:true+detached). Extension 168→180 (+12). Full suite 913→925 pass + 17 skip. Build + lint clean. **CEILING (recorded, not overstated): Linux-native = correct-by-construction + mocked-platform-test-covered; LIVE-UNCONFIRMED** — real unix-socket connect / detached-spawn lifetime / bare-binary PATH resolution need a real Linux host (P4 cross-platform CI). Sub-band because the refactor was localized (defaults-preserving, one use-site for the const→fn) and the test surface mirrored existing `daemon-lifecycle.test.ts` idioms (FakeChild, injected spawn). |
| CB-CLEAN-BUILD-FIX | build/dependency fix + clean-build restoration | (none stated; analog: dependency/build diagnosis + fix ~15-30 min) | **≈17 min — MEASURED** (epoch 1780756202 dispatch-receipt → 1780757206; 1004s = 16.7 min) | in-band-low (vs ~15-30 analog) | **Root cause: corrupted `node_modules` — `@modelcontextprotocol/sdk@1.29.0` (and, found mid-task, `vitest`) installed WITHOUT their `dist/` payload** (`files:["dist"]` present in metadata, dist absent on disk); every SDK subpath import (`./server/index.js` etc., resolved via the `./*`→`./dist/esm/*` exports wildcard under NodeNext) pointed at nonexistent files → TS2307. **Reproduced in my env too** (not operator-specific) — the exact 4 errors. `package.json` + lockfile were CORRECT (SDK properly declared+locked); the fix was a deterministic **`npm ci`** clean reinstall (targeted SDK-only reinstall was insufficient once vitest's corruption surfaced). **Why hidden for weeks:** incremental `tsc -b` skipped recompiling the daemon (stale `.tsbuildinfo` → "up to date") and reported success while emitting stale dist — a green incremental build was never evidence the project builds (pool finding #1; honestly, this session's own T-P3-006/07/08 build confirmations were incremental-masked too). **Premise correction:** the global `claude-bridge` is a **symlink → the repo** (verified `readlink`), NOT a stale copy — so the operator's "stale daemon" ran the repo's stale dist (build-masking), and after the clean rebuild the symlinked global is automatically current (positive confirmation: global daemon dist = repo dist, 006/007/008 + 12 canon matches present — by file inspection, not log-absence). **007 startup-log question SETTLED:** the interaction log emits NO startup line — `AuditLog` opens its file lazily on first `append()` and `InteractionRecorder`'s ctor does nothing; this is consistent with the original `audit.jsonl` (both AuditLog sinks are the exception to main.ts's "X initialized" log convention). Daemon freshness must be confirmed by build inspection / the `daemon starting` version line, never by an interaction-log startup line (there is none). **Durable safeguard added** (the only repo delta): `scripts/clean.mjs` (dependency-free, cross-platform Node `fs.rmSync`) + `clean`/`build:clean` npm scripts, so `npm run build:clean` makes the clean-build the dispatch demands a one-command, CI-ready step. Verified end-to-end (removes 8 artifacts → rebuild exit 0). Full suite 925 pass + 17 skip; lint clean. The build fix itself needed NO repo change (corrupted store, not a code/manifest bug). |
| CB-OAUTH-DISCOVERY-FIX | OAuth discovery-endpoint fix + chain test | (none stated; analog: spec-correct endpoint add + integration test ~15-30 min) | **≈10 min — MEASURED** (epoch 1780758919 dispatch-receipt → 1780759521; 602s = 10.0 min) | sub-band-low (vs ~15-30 analog) | **Root cause: `/.well-known/oauth-protected-resource` was NOT IMPLEMENTED** — the `oauthHandler` (which runs BEFORE the Bearer `authenticate()` and handles metadata/register/authorize/token) had no route for it, so it fell through to the MCP auth gate → 401. Asymmetry explained: the RFC 8414 auth-server metadata HAS a route (public) but the RFC 9728 protected-resource doc had none → blanket 401. claude.ai fetches the RFC 9728 doc as the FIRST discovery step, so it aborted the connector flow with a generic "couldn't register" before ever reaching `/register` (which the smoke proved works, 201). **Fix (minimal, spec-correct):** new `oauth/protected-resource.ts` (RFC 9728 `{resource, authorization_servers:[issuer], bearer_methods_supported:["header"]}`, Host-derived like metadata.ts, reuses `deriveBaseUrl`) + `OAuthProtectedResourceMetadataSchema` in shared + one router line (public, mirrors the metadata route). **Discovery-route audit:** all `.well-known`/discovery/DCR/authorize/token routes run in the pre-auth oauthHandler → correctly public-at-HTTP-layer; only the MCP tool fall-through requires the token. No auth loosened. **Tests:** `protected-resource.test.ts` (6: shape + Host-rotation + schema-valid) + 3 router tests incl. the **discovery-CHAIN test** that walks protected-resource → auth-server → DCR as an UNAUTHENTICATED client (no Bearer), asserting each step reachable + one coherent issuer + bogus-token-doesn't-change-200 — the gap that let the bug through (every endpoint passed its own unit test; no test replayed the client's multi-step chain). Daemon 577→586 (+9); full suite 934 pass + 17 skip; clean build (`build:clean`) + lint green. Sub-band: the fix mirrored the existing public-metadata route exactly (one new handler + one router line) and the spec shape was small/unambiguous. |
| CB-DAEMON-LIFECYCLE-FIX | daemon lifecycle + reconnect-registration + status-surfacing (multi-part) | (none stated; analog: multi-part lifecycle/IPC fix + tests ~25-40 min) | **≈23 min — MEASURED** (epoch 1780764266 dispatch-receipt → 1780765620; 1354s = 22.6 min) | in-band-low (vs ~25-40 analog) | The live-smoke consent blocker, 3 coupled parts (recon-isolated). **(c1, the blocker):** the extension auto-reconnected but never RE-registered (state stayed "registered" → daemon's per-socket activeRegistry empty after any restart/EPIPE → /authorize extension_offline while the status bar lied "registered"). Fix: `onConnectionStateChanged` re-arms "registered"→"registering" on disconnect so the next "connected" re-sends register_workspace; user-decision states (needs_trust/trust_denied/duplicate) untouched. **Key test** (required proof): mock disconnect+reconnect → asserts register_workspace re-fires. **(a) single-instance:** promoted the TCP port to the AUTHORITATIVE cross-platform lock (`isDaemonPortListening` connect-probe run BEFORE the pid write — the win32 named pipe permits multiple listeners, pid-liveness is presence-only). LOCKED decision: doubled start REFUSES, incumbent stays in force (no auto-kill, no --force); port is authoritative over the (now diagnostic-only) pid file. Reliable `stop`: verifies termination, escalates SIGTERM→SIGKILL on hang, cleans the pid file (was: print "Stopped" on signal-receipt) — with a self-pid guard (daemon pid ≠ CLI pid in real use). **(b) visibility:** StatusPayload.connected_extensions {identifier,pid,abs_path} from getActiveRegistry() (was hardcoded attached_workspaces:0); hello_ok carries daemon_pid → extension status-bar tooltip shows the bound daemon. **(c2) legibility:** offline page distinguishes "no extension here" vs "connected but unregistered" (+ enumerates all four causes, points at `claude-bridge status`). Both new wire fields OPTIONAL (mode-precedent compat; relevant given the recent stale-build episode). cli 53→59, daemon 586→593, extension 180→182 = 949 pass + 17 skip; build:clean + lint green. In-band-low: 4 parts but each localized + recon had pre-resolved the design; test surface (15 new) was the bulk. |
| T-P3′-0 | path-normalization spike (SPIKE: daemon fn + extension probe + operator-gated live capture + test) | optimistic ~10 / **expected ~18 min** (dispatch dual-band, excl. operator-gate wait) | **≈18 min — ESTIMATED (hands-on, excl. operator-gate wait)** — t0 epoch NOT captured at literal receipt (process miss); reconstructed from anchors: pre-gate ended at VSIX mtime 13:21, post-gate test-runs 13:27–13:28 → cal-log 13:30. Pre-gate (recon 8 files + fn + probe + wire + build:clean + VSIX) ≈13 min; post-gate (calibrate + live-fixture test + recon note + cal-log + final build) ≈5 min. Operator-gate wall-clock wait EXCLUDED. | on-band (vs expected ~18) | **Hypothesis HELD** — VS Code fsPath lowercases the drive letter (`C:`→`c:`), backslash seps, no trailing slash, exactly the dispatch's starting guess; **load-bearing nuance captured:** fsPath PRESERVES segment case (`Projects` kept capital-P), so canonicalize must NOT fold segment case — that's `normalizeAbsPath`'s job (Phase 2b). win32 = GROUND-TRUTH (live capture, folder `C:\Projects\clyde_claude_bridge` → `c:\Projects\clyde_claude_bridge`); posix = DERIVED/LIVE-UNCONFIRMED (no Linux host, P4 closer); UNC = NOT exercised (drive path), arm left as flagged hypothesis. `canonicalizeWorkspacePath(input, 'win32'\|'posix')` in shared/path.ts beside `normalizeAbsPath` (platform injected, mirrors `candidatesFor`); extension probe command `claudeBridge.probeWorkspacePath` (verbatim 3-field OutputChannel). Test: 8-variant equivalence table → live-captured fixture byte-for-byte + negative segment-case guard + posix mock arm. **SPIKE only — no production wiring** (advertise/discover = Phase 2b). **Transient flake noted:** first full `vitest run` on cold transform crashed all 9 shared suites ("No test suite found", 14.5s transform); reproduced GREEN on immediate re-run (131 pass) — watch-item, not pooled (pool deferred). Process miss (t0 epoch) = restore epoch-stamp-at-receipt as a hard first action. **Recon finding:** `--workspace` is NOT parsed by the CLI today → fn has no current call site (correct for spike). |
| T-P3′-1a | required --workspace/--name + canonical daemon identity (daemon-side, fire-and-run) | empirical 10–16 (mid ~13); legacy 15–30 | **≈13 min — MEASURED, CLEAN** (t0=2026-06-07T18:05:52Z via §0 first-command stamp → commit ~18:19Z; ~13 min, no operator gate) | in-band (lands ON the ~13 empirical midpoint) | **First clean measured P3′ fire-and-run datapoint — and the first live test of the NEW C-35 binding** (t0 stamped by the literal §0 first command, paired with git-status; no estimate-grade reconstruction, unlike gated T-P3′-0). **Composition wired (the load-bearing decision):** `identity = normalizeAbsPath(canonicalizeWorkspacePath(input))` — case-FOLDED key; `display_path = canonicalizeWorkspacePath(input)` case-PRESERVING; both platform-injected. **AC-1a-7 headline proof GREEN:** 9 equivalent --workspace forms (uppercase/lowercase/ALL-CAPS drive+segments, fwd/back/dup separators, trailing slash, mixed) all fold to the SAME identity `c:\projects\clyde_claude_bridge` — the Phase 2b case-folded-key contract proven early. **Read-first finding:** `--workspace`/`--name` did NOT exist in the CLI (start took no args; daemon read config.workspace from config.json) — net-new, NOT "likely from P3"; `normalizeAbsPath` was complete (not a stub) but not platform-injectable → added optional platform arg (behavior-preserving for the WorkspacesStore caller). **Surface:** new daemon `workspace/identity.ts` (computeDaemonIdentity) + main.ts identity-log at step 2.5 (logged BEFORE single-instance/tunnel gates so it emits even on refused/tunnel-less start) + argv `--workspace`/`--name` parse (optional in daemon → acceptance/direct invocation untouched); CLI `start` gains required --workspace (single-folder, collected-array dup detection) + --name with 4 typed errors (WorkspaceRequired/NameRequired/MultipleWorkspace/WorkspaceNotADirectory) via testable `resolveStartArgs` + spawn-forwarding. cli 73→80 (+7), daemon 603→616 (+13), shared 131→134 (+3); extension 190 unchanged. **Real startup invocation (verbatim, currency-positive):** daemon emitted `{"msg":"daemon identity","identity":"c:\\projects\\clyde_claude_bridge","name":"clyde-dev","workspace_path":"c:\\Projects\\clyde_claude_bridge"}` then stopped gracefully (pid cleaned) — the NEW line confirms the fresh build ran (007 lesson: positive confirmation, not absent-log). All 4 CLI error paths verified end-to-end (clear msg + exit 1). **AC-1a-8 held:** no resource-scoping/lock/port change (1b/1c). build:clean + lint green. In-band: composes existing shared/path.ts fns (low discovery, rules proven in Phase 0) — exactly the predicted "stays under T-P3-007 ~17 because it composes not builds". |
| T-P3′-1b | per-daemon config-dir/port/pipe + state-dir migration (heaviest P3′ phase; daemon-side, fire-and-run) | empirical 18–28 (mid ~23); legacy 30–60 | **≈30 min — MEASURED, CLEAN** (t0=2026-06-07T19:09:56Z §0-stamped → commit %cI 2026-06-07T19:40:04Z; 30.1 min; no operator gate) | **marginally OVER empirical upper (~28); at legacy floor (~30)** — informative re: migration-shape cost, not a process miss (dispatch: out-of-band-high here is expected of the heaviest/first-of-shape phase) | **Second clean measured P3′ datapoint; the heaviest phase, landed AT/just-over the empirical ceiling as the wide band anticipated (migration breadth + the risk-verification surfacing a real finding that required a re-test).** **Derivation (frozen):** identity→`sha256(identity)[:16]`→configDir `%APPDATA%\claude-bridge\<hash>\` + pipe `\\.\pipe\claude-bridge-<hash>`; port = `--port` (explicit, errors if taken) else next-free from config port; legacy/no-`--workspace` mode unchanged. Shared `workspaceIdentityKey` (the one identity composition daemon+CLI agree on, pure/no-crypto); hash duplicated daemon `resources.ts` ↔ cli `util/paths.ts` (the established cli↔daemon-boundary convention) with a known-fixture cross-package test guard (`135cfaa3d11a768c`). **Migration:** paths.ts fns gained optional `configDir` (default flat = behavior-preserving); main.ts resolves per-daemon dir from `--workspace`, threads it into config load/init + all stores (workspaces/clients/tokens) + transcripts/interaction + IPC addressOverride (per-daemon pipe); sdk-runner floor auth-dir → `this.configDir` (per-daemon, not flat root); CLI `start` derives the same dir+pipe so its post-spawn status/token read works. **config.workspace read RETIRED** (single source = `--workspace`; validator now unreferenced). **LIVE two-daemon (win32, GROUND TRUTH):** sequential `start A(clyde)`+`B(ClaudeDiss)` → distinct hash/dir/port(7423,7424)/pipe, both ran, separate dirs no shared writes (filesystem-verified); same-workspace restart → same dir, NO first-run-init, token persisted (AC-1b-6). **AC-1b-9 FINDINGS (→1c):** (1) port-lock no longer blocks different-workspace daemons (next-free) — desired isolation but old "2nd-start refuses" gone; (2) **concurrent-start port TOCTOU race** — simultaneous starts both grabbed 7423, loser died at MCP-bind EADDRINUSE (sequential is race-free; `start` waits-for-ready so scripted starts serialize) — bind-with-retry deferred to 1c; (3) same-workspace double-start UNGUARDED (pid warn-only, port-guard neutralized) — 1c must make per-daemon pid authoritative. No lock re-scope/redesign this task (characterize-only per dispatch). **No auto-migration** (clean cutover; flat tokens.json orphaned, operator re-binds). **Extension auto-connect breaks until 2b** (intended, scope d — pipe in startup log for interim). cli 80→84, daemon 616→629, shared 134→136 (+19 incl. derivation determinism/isolation/restart/port-alloc + cross-package fixture guard); extension 190 unchanged. build:clean + lint green; no regressions (existing legacy-path tests preserved via default-arg behavior-preservation). Upper-band because the migration touched every state consumer + the risk AC genuinely bit (race finding → sequential re-verification), exactly the "risk-verification complexity could push it high" predicted. |
| T-P3′-1c | identity-keyed single-instance lock + atomic bind-with-retry (daemon-side, fire-and-run; closes AC-1b-9 holes) | empirical 14–22 (mid ~18); legacy 15–30 | **≈26 min — MEASURED, CLEAN** (t0=2026-06-07T20:19:41Z §0-stamped → commit %cI 2026-06-07T20:45:21Z; 25.7 min; no operator gate) | just OVER the empirical upper (~22), within legacy (~26) — the concurrency-testing variance driver bit exactly as predicted (a flaky external pipe-poll probe forced a full re-verification with process-liveness + ready-log signals) | **Third clean measured P3′ datapoint.** **Lock mechanism (the §0b2-driven choice):** connect-probe the per-daemon IPC pipe/socket (`isIpcAddressLive`) — liveness-true, OS-released on crash, UNIFORM across win32/posix. Chosen because reading `IpcServer.start()` confirmed the dispatch's caveat: **win32 named pipes allow multiple listeners** (no FIRST_PIPE_INSTANCE, no win32 stale-cleanup) so pipe-*bind* can't lock there; a *connect*-probe sidesteps it (a live listener answers regardless). Acquired EARLY (before port/MCP) so a refusal has zero side effects. **ITEM 2:** bind-with-retry in `McpServer.start` (autoAllocate) — the BIND is the claim, retry-on-EADDRINUSE closes 1b's probe-then-bind TOCTOU; explicit `--port` keeps `ExplicitPortInUseError` (no retry). Replaced 1b's racy `allocatePort` (removed) with `bindWithRetry`. **LIVE GROUND TRUTH (win32):** refuse-live (dup→exit1, incumbent pid 24240 untouched, no shutdown) · kill-9→reclaim (new pid, zero refusals — liveness not stale-pid decided) · **concurrent different-workspace start → 7423+7424 both up, ZERO EADDRINUSE deaths** (1b killed one here — the race is closed) · explicit --port 7500 taken → ExplicitPortInUseError exit1. **Deterministic proof:** `bindWithRetry` mock-EADDRINUSE unit test (advances 7423→7424→7425, asserts attempts) + `isIpcAddressLive` real-ephemeral-server probe test. **Read-first:** 1b's "guard" was a vacuous port check + warn-only pid (confirmed); legacy/no-`--workspace` mode keeps the original port guard. **Residual (narrow, noted):** simultaneous SAME-workspace starts have a sub-second pre-IPC window; real `start` waits-for-ready so the operator flow is race-free; OS-early lock deferred. daemon 616-era→ +16 net (resources lock/bind-retry/probe + McpServer autoAllocate; allocatePort tests → bindWithRetry+isIpcAddressLive tests); cli/shared/extension unchanged. build:clean+lint green; no regressions (McpServer default path behavior-preserved). Upper-edge: two focused mechanisms were quick, but the timing-dependent live concurrency verification + the flaky-probe re-run consumed the margin — "concurrency testing is the variance driver" confirmed. |
| T-P3′-2a | daemon advert lifecycle (write/remove/reclaim) + cross-daemon handshake-gated sweep (daemon-side, fire-and-run; live-verification shape) | empirical 16–24 (mid ~20); legacy 20–30 | **≈17 min — MEASURED, CLEAN** (t0=2026-06-07T21:37:13Z §0-stamped → commit %cI 2026-06-07T21:54:25Z; 17.2 min; no operator gate) | LOWER edge / below midpoint (empirical 16–24) — the advert mechanics were clean and the tricky parts (retry gate, idempotency) were unit-tested, so the live-verification tax landed light | **Fourth clean measured P3′ datapoint; came in UNDER the (live-verification-priced) band — a useful counterpoint that not every live-verify task pays the full ~3-5 min tax when the mechanics are simple.** **Advert schema (scope a, local-pairing only):** `daemons/<hash>.json` at the TOP-LEVEL shared dir (not per-daemon) = {canonical_workspace (case-folded identity = 2b match key), name, pipe, port, pid, started_at} — **NO tunnel URL** (ADR-002, deferred to stable-tunnel). `DaemonAdvertSchema` in shared (2b consumes it). **Load-bearing sweep invariant (scope c):** delete ONLY on FAILED HANDSHAKE, never pid/age; `isAdvertLive` = 1c's `isIpcAddressLive` connect-probe (reused) × 2-attempt retry → live-but-slow survives; idempotent (concurrent-boot ENOENT swallowed); unparseable advert LEFT intact (can't confirm dead); own advert skipped via selfHash. **Reclaim-own UNIFIED not duplicated:** writing `<hash>.json` overwrites = generalizes the 1b/1c pid-overwrite reclaim. Write+sweep after `ipcServer.start()` (pipe live), best-effort, per-daemon mode only; remove on graceful exit beside `removePidFile`. **LIVE GROUND TRUTH (win32):** write→schema present · graceful stop (via IPC `stop`)→advert removed · kill-9→stale advert→restart reclaimed (fresh pid) · sweep: A live + planted dead advert → B's boot logged `removed dead advert … swept:1 kept:2`, dead gone, A+B intact. **Deterministic safety proof:** unit tests for retry-keeps-slow (AC-2a-5), idempotent (2a-6), durable-untouched (2a-7), unparseable-left. **Verification-methodology finding (not a code bug):** win32 `process.kill(pid,'SIGTERM')` is a hard TerminateProcess (no catchable signal) — graceful shutdown only fires via the IPC `stop` path; needed a file-based pipe-stop helper (the heredoc mangled the `\\.\pipe\` backslashes). daemon 630→643 (+13 advert tests); shared schema exercised via daemon tests (shared 136 unchanged); cli/extension unchanged. build:clean+lint green; no regressions. |
| T-P3′-2b | extension discovery + auto-pair (first extension code since P0; VSIX redeploy + operator-reload; restores auto-connect) | empirical 18–28 (mid ~23); legacy 25–40 | **≈24 min — MEASURED, hands-on EXCLUDING the operator-reload wait** (t0=2026-06-07T22:16:31Z §0-stamped → commit %cI 2026-06-07T22:40:14Z; 23.7 min; clock-paused at reload per §10.8 — committed before the operator reload) | in-band (mid/upper empirical ~18–28, as predicted for the redeploy + live-verify shape) | **Fifth measured P3′ datapoint; first NON-fire-and-run (extension redeploy + operator reload).** **Discovery flow:** extension `workspaceIdentityKey(workspaceFolders[0].fsPath)` → scan `daemons/` → byte-match `canonical_workspace` → `setEndpoint(advert.pipe)` + connect via the REUSED hello/register handshake (registration.ts UNTOUCHED — handshake = liveness; stale advert fails it → no-match, not deleted). Either order via `startPairing` poll (2s; chose polling over fs.watch for win32 robustness); per-daemon pipe is stable so discovery is once-only (reconnect loop owns liveness after). **Build-id currency (scope c):** esbuild injects `__CB_BUILD_ID__`=`<ver>+<base36 time>`; extension sends it in hello; daemon logs `extension build connected {build_id}` — machine-verified running==built, retires the reload-eyeball. **Multi-root (scope b):** [0]+notice. **NEW vs REUSED:** new `discovery.ts` + `IpcClient.setEndpoint` + activate pairing block + multi-root notice; everything else reused (handshake, reconnect, status bar — no rewrite, scope a). **LIVE GROUND TRUTH (mechanism, via a sim mimicking the extension):** byte-match TRUE (fsPath `c:\Projects\…`→identity `c:\projects\…`===advert canonical_workspace; uppercase variant→same identity→MATCH) · HELLO_OK on advert.pipe · daemon logged the REAL built build-id `0.1.0+mq4cz57w` · stale (kill-9): match holds but handshake `connect ENOENT`→no connect, advert not deleted by extension. **VSIX rebuilt + `code --install-extension … --force` ("successfully installed"); build-id baked into dist confirmed.** **VS-Code-host currency (real extension paired + AC-2b-7 from the running editor) = operator-reload residual** (handed to Jay; the daemon will log `0.1.0+mq4cz57w` on reload). **Tests:** discovery byte-match/variant/stale-skip/watch-appear/match-once (9) + hello-carries-build_id (1) + multi-root notice (1). ext 190→201, shared 136 (schema in 2a). build:clean+lint green; no regressions. In-band: the live-verify tax (byte-match + build-id sim + stale, much pushed into deterministic checks) landed where the redeploy+live-verify band predicted — validates the 2a band-refinement direction (this one paid more tax than 2a, as predicted for the heavier live/redeploy split). |
| T-P3′-3 | extension UI: two-segment status bar + no-pairing surface + derived-args spawn + detached-survival (largest P3′ dispatch; VSIX redeploy + operator reload; +scope-correction addendum mid-flight) | empirical 28–40 (mid ~34); legacy 40–60 | **≈34 min — MEASURED, hands-on EXCLUDING the operator-reload wait** (t0=2026-06-07T23:38:32Z §0-stamped → commit %cI 2026-06-08T00:11:52Z; 33.3 min; clock-paused at reload per §10.8) | in-band — landed ON the empirical midpoint (28–40), exactly as predicted for the largest/heaviest-live-split dispatch | **Sixth measured P3′ datapoint; the biggest. Predicted midpoint hit dead-on — the live-verify-tax band model holds at the top end too.** **Two-segment bar** `[daemon] $(arrow-both) [claude.ai]`, tooltip just `click for commands` (state inline). Daemon states = handshake reality (no stale/waiting): `<name>(pid)·live` / `connecting…` / `version mismatch` / `not running — start from palette` (no advert) / **`found but workspace mismatch`** (adverts present, none match — the case-fold canary). **ADDENDUM mid-flight:** re-scan command dropped (redundant w/ the 2b poller), its near-miss diagnostic folded INTO the bar as the mismatch state — net LESS code (removed a half-built reScan). **Menu = exactly Start daemon + Unbind** (stripped mode/trust/retry/open-url/copy-id/stop-hint). **Derived-args spawn (AC-3-5/6/8):** `deriveSpawnArgs(fsPath)`→`--workspace <canonicalize(fsPath)>`+`--name <basename>`→ daemon advertises → 2b discovery pairs; spawn does NOT connect itself, promises ONLY "up+advertising" (no tunnel/connector/binding). **§0b3 ANSWERED (long-open):** current terminal start = `shell:true` (cmd.exe runs the `.cmd` shim, NOT PowerShell/`cmd /c start`) + detached + unref; the CLI double-detaches the daemon. **AC-3-7 detached survival PROVEN (the unknown):** reproduced the idiom, parent waited-for-advert then EXITED (VS Code-close sim) → daemon stayed ALIVE + ADVERTISING (next-free port 7424). win32 shell:true+args → DEP0190 (concat not escaped) → value args double-quoted for spaces. **Discovery poller now CONTINUOUS** (onScan feeds discoveryTotal so the bar stays truthful after a daemon dies; onMatch connects once). **Heavy test rewrite:** status-bar + status-bar-menu rewritten for the new model; daemon-lifecycle startDaemon gains a target arg (8 call sites + 2 args assertions + runStartDaemonCommand workspaceFolders); discovery startPairing onScan/ScanResult; +deriveSpawnArgs + multi-root + near-miss tests. ext 201→169 net (old per-state tests collapsed into the leaner two-segment model; -32 but denser). **Verification-hygiene slip + recovery:** accidentally cleared the daemons dir (deleting Jay's LIVE advert) → my spawn hit AC-3-9 (1c refused the dup) → restored Jay's advert from its pidfile + redid the smoke on a throwaway ws. build:clean+lint green; full suite green (daemon 643, cli 84, shared 136, ext 169). VSIX rebuilt (build-id 0.1.0+mq4gfctn, baked-confirmed) + `--install-extension --force` ("successfully installed"); **from-real-editor currency = operator-reload residual** (daemon will log 0.1.0+mq4gfctn on Jay's reload). `makeDaemonNotRunningHandler` unwired (retry surface stripped) but kept+tested (later-removal candidate). |
| T-P3′-4 | orphaned-binding reclaim: always-takeover on fresh consent (shared schema + daemon + extension; VSIX redeploy + operator reload; LAST in P3′) | empirical 24–34 (mid ~29); legacy 30–55 | **≈16.3 min — MEASURED, hands-on EXCLUDING the pre-flight read + operator checkpoint pause** (t0=2026-06-08T03:59:06Z §0-stamped at the consolidated-dispatch receipt → commit %cI 2026-06-08T04:15:22Z; 16.3 min) | **SUB-BAND-LOW (vs empirical 24–34)** — the §0(b) read + the captured-set design landed in the pre-flight (a productive checkpoint the operator folded into the consolidated dispatch), so the MEASURED build was a clean focused inversion at one seam; the live-verify tax is partly deferred to the operator reload | **Seventh + LAST measured P3′ datapoint; the build sequence closer.** **The inversion (recon YELLOW item 1):** refuse-when-bound (`no_unbound_workspace`, consent.ts:253-261, delivered-via `broadcastServerMessageToUnbound`) → **always-takeover**: when every active window is already bound, the send adapter (main.ts) now delivers a TAKEOVER consent to the bound window via a new `IpcServer.broadcastTakeoverToBound` (per-window message so each carries its own old-binding disclosure). **Captured-set install-then-revoke (decision c — the checkpoint correction):** `revokeByWorkspace` could NOT be reused (it revokes ALL workspace tokens → would kill the just-minted new token = the unbound state). New mechanism at /token redemption: capture the old token hashes for the workspace BEFORE minting → mint new → `revokeByTokenHashes(captured)`. The new token is never in the captured set BY CONSTRUCTION (captured pre-mint) — no timestamp/issued-before edge. **Uniform + behavior-preserving:** a FRESH bind has an empty captured set → revoke is a no-op → unchanged; only a takeover (re-bind of an already-bound ws) revokes. Per-workspace, so it never touches another workspace's binding (the AC-12 isolation property holds at the revoke layer too). **`revokeByWorkspace` LEFT UNTOUCHED** for the manual `unbind` path (AC-4-8). **Both-bindings disclosure (decision a):** the modal is INTERROGATIVE ("Replace … ?") and shows OLD (client/bound-date/expiry) + NEW (requesting client identity, pre-mint — NO fabricated new-token date) so same-client re-bind vs different-client takeover is visible (the recognize-or-reject security signal). **Modal is extension-rendered** (`composeConsentModalText`) → shared schema (`AuthConsentRequest.takeover?`) + daemon + extension changed → VSIX redeploy. **No-race-by-design (decision d):** one window per daemon (ADR-001) → the dormant dismiss-race can't fire; untouched. **8 src/test files** (shared schema; token-store +3 methods; ipc/server +broadcastTakeoverToBound; main adapter; token-endpoint capture-mint-revoke; consent comment; ext modal). **Tests +12:** token-endpoint takeover/ordering-safety (+5: install-then-revoke, fresh-bind-unchanged, per-ws-isolation, install-fail-leaves-old, revoke-fail-leaves-new via vi.spyOn), token-store captured-set (+3), ext modal both-bindings (+4). **AC-4-7/4-8 GUARDRAIL:** the AC-10/AC-12 isolation + unbind/`revokeByWorkspace` test files VERIFIED UNMODIFIED (git diff --quiet) and green — the takeover adds reclaim WITHOUT weakening isolation. daemon 643→651 (+8), ext 170→174 (+4), cli 89/shared 136 unchanged. build:clean+lint green. VSIX rebuilt (build-id 0.1.0+mq4p4b68, baked-confirmed) + `--install-extension --force` ("successfully installed"); **from-real-editor build-id currency = operator-reload residual** (daemon will log 0.1.0+mq4p4b68 on reload). **Candidate captured this session:** `unreachable-prod-code-may-be-adversarial-test-scaffolding` (the 4pre abort; pushed cf7f6bc) — none NEW in 4 itself. Sub-band-low because the design was pre-resolved at the checkpoint (the captured-set wrinkle surfaced + confirmed before building) — a clean example of pre-flight reducing build-time variance. |
| T-P3′-5 | extension menu: Stop daemon + approval-mode switch with clamp-to-most-restrictive resolution (shared + daemon + extension; VSIX redeploy + operator reload) | empirical 22–32 (mid ~27); legacy 30–50 | **≈34.1 min — MEASURED, hands-on** (t0=2026-06-08T05:26:35Z §0-stamped → commit %cI 2026-06-08T06:00:39Z; 34.1 min; no operator gate mid-build, reload residual after) | **marginally OVER empirical upper (~32); at legacy floor (~30)** — the security-critical clamp + the multi-package menu/IPC plumbing (8 src files across all 3 packages) consumed the upper margin, exactly the "the security-correction is the part to get right" predicted weight | **Eighth measured datapoint; the menu-completion + security-correction task.** **Two menu items (recon-confirmed gaps):** **Stop daemon** (always-shown) — targets the paired daemon via the discovery advert, sends `{kind:"stop"}` over the EXISTING IpcClient socket → daemon `handlers.stop()` → graceful `shutdown()` (no shell-out); confirm-prompt for the live-connection disruption. **Set approval mode** (when-bound) — per-workspace granularity QuickPick (per_call/task/auto, current shown) → `{kind:"set_granularity"}` → `TokenStore.setGranularityForWorkspace`; binding-default mint changed null→**per_call** (the switch is never unset). **THE security correction (AC-5-5/5-6):** `resolveOperationGranularity` inverted from **override-first → CLAMP-TO-MOST-RESTRICTIVE**: effective = `moreCautiousGranularity(operator-ceiling, claude.ai-request)` on the rank per_call>task>auto. The operator switch is a CEILING claude.ai can tighten under but NEVER loosen past — **switch=per_call + request=auto now resolves per_call** (the named can't-loosen test), fixing the prior privilege-escalation-shaped weakness where the GATED party chose its own gate level. Override-first is fully GONE from the bound path (the only surviving `return callGranularity` is the legacy no-switch Bearer path, which has no ceiling). **Clamp lives in shared** (`moreCautiousGranularity`, rank-based). **8 src files:** shared (clamp helper + `set_granularity`/`set_granularity_ok` schema + `binding_established.granularity`), daemon (gate clamp; mint per_call; token-store setGranularity/granularityForWorkspace; ipc/server `set_granularity` socket handler + `setGranularitySetter`; main wiring; consent binding_established granularity), extension (BindingInfo.granularity; menu Stop+Set-mode items + executor; extension.ts stop/setGranularity adapters + onBindingEstablished). **Tests +16:** clamp matrix all-5-cases incl. the named SECURITY can't-loosen (gate; replaced the 1 override-first test), mint-per_call + non-binding-null (token-endpoint +2), setGranularity-persists-survives-reload (token-store +2), moreCautious clamp (shared +2), menu Stop-always/Set-mode-when-bound + stop-confirm/cancel + set-mode/cancel (extension +6). **AC-5-7 GUARDRAIL:** AC-10/AC-12 isolation + unbind test files VERIFIED UNMODIFIED (git diff --quiet) + green. daemon 651→659 (+8), ext 174→180 (+6), shared 136→138 (+2), cli 89 unchanged. build:clean+lint green. VSIX rebuilt (build-id 0.1.0+mq4swp0u, baked-confirmed) + `--install-extension --force` ("successfully installed"); **from-real-editor build-id currency = operator-reload residual.** Upper-edge: the clamp is small but security-load-bearing (full 5-case matrix + override-first-gone proof), and the plumbing spanned all three packages + a new socket-scoped IPC round-trip — breadth, not depth, drove the time. |
| T-TUNNEL-1 | tunnel lifecycle ownership + operator-gated drop recovery (daemon + shared + extension; 2-commit split-seam; VSIX redeploy) | (A) empirical 12–20 / (B) empirical 18–28 (mid ~23); legacy Large 40–65 combined | **(B) ≈23.4 min — MEASURED, hands-on** (t0=2026-06-10T01:17:33Z §0-stamped at the (B) receipt → commit 2 %cI 2026-06-10T01:40:55Z; 23.4 min — dead on the (B) midpoint). (A) shipped separately (`ae06c2b`, daemon-only). Per the split-seam, this row times (B); (A)'s ownership-hygiene time is excluded (committed earlier, the operator-checkpoint gap between is excluded too) | **(B) on-band (empirical 18–28, near mid)** — the never-silent-adopt structural guarantee + the modal round-trip across 3 packages + the no-extension fallback landed where the (B) band predicted | **The deferred-cluster's biggest item, split A/B as the dispatch's seam anticipated.** §0(b) ground-truth REFINED the symptom-inferred dispatch: graceful teardown was **already fast on win32** (immediate child.kill) — not the bug; the real gaps were windowsHide + pid-persistence + reclaim-on-startup (A) and the silent-adopt in attemptRespawn (B). **(A) ownership invariant (`ae06c2b`, daemon-only, no redeploy):** launch hidden (windowsHide) + owned-not-detached; cloudflared pid → tunnel.pid on each (re)spawn via a new `pid_change` event, cleared on graceful stop; **reclaim-orphan-on-startup (PRIMARY** — hard-kill bypasses shutdown() on win32, so this not graceful teardown is the load-bearing orphan defense); kill-before-respawn explicit. **(B) operator-gated drop recovery (this commit):** the gate is `attemptRespawn` — a drop-respawn emits **`url_pending` (NOT url_change)**, so the rotated quick-tunnel url is **held out of state.tunnelUrl until the operator confirms** (the named AC-T-7 structural never-silent-adopt: only DropRecovery.onDecision('confirm') calls adopt). Modal reuses the consent daemon-initiated channel (`tunnel_drop_request`/`tunnel_drop_response`): confirm → adopt (promote pending→live); **deny → tunnelManager.stop() (rides (A)'s clean teardown — no orphan), sit tunnel-less.** No-extension-at-drop → pending held, surfaced in CLI `status`, modal **re-fires on next extension connect** (a connect hook in the hello handler). Dismiss ≠ decision (stays pending). **The never-silent-adopt guarantee is STRUCTURAL** — DropRecovery's onDropRespawn only records+asks; there is no respawn→adopt path without a confirm. **AC-T-9 GUARDRAIL:** AC-10/AC-12 isolation test files VERIFIED UNMODIFIED (git diff --quiet) + green. (A) daemon 659→665 (+6); (B) daemon 665→672 (+7: drop-recovery 6, manager url_pending 1), ext 180→184 (+4 tunnel-drop modal), shared 138, cli 89. build:clean+lint green. VSIX rebuilt (build-id 0.1.0+mq7ei81x, baked-confirmed) + `--install-extension --force` ("successfully installed"); **from-real-editor build-id currency = operator-reload residual.** **Closes T-TUNNEL-1 / the tunnel-lifecycle deferred cluster.** On-band: the gate + the testable DropRecovery core (mocked deps) kept the security-critical never-silent-adopt unit-fast; the 3-package modal round-trip + the connect-hook fallback were the breadth. |
| T-CLI-1 (commit 1) | unified daemon selector + per-daemon conversion of the flat verbs (CLI-only; commit 1 of 2, list/delete-dir = commit 2) | empirical 24–34 (mid ~29); legacy Medium-Large 30–50 | **≈24.3 min — MEASURED, hands-on** (t0=2026-06-10T02:01:33Z §0-stamped → commit %cI 2026-06-10T02:25:49Z; 24.3 min; no operator residual — CLI-only) | **ON the empirical low edge (~24)** — the recon pre-mapped every build site, so the selector was the only design work and the 5 verb conversions were mechanical route-through-the-selector edits | **CLI multi-daemon conversion, commit 1.** Built THE unified selector `selectDaemonTarget` (`util/selector.ts`): **--name resolution** (friendly name → canonical workspace via advert enumeration, reusing the statusEnumerate pattern as a shared `enumerateAdverts`) + the **§2 asymmetric default as ONE shared function** (bare → act-on-sole if one daemon / AmbiguousDaemonError-and-list if many / NoDaemonsRunningError if none — never the flat fallthrough). Converted **stop, tail-log, token rotate, tunnel restart, unbind** to route through it instead of `getCliPidPath()`/`getCliConfigPath()` flat; added `--workspace`+`--name` flags to all daemon-addressing verbs (index.ts). **THE fix (AC-C1-3, named test):** bare `stop` no longer reads the empty flat pid path and misreports "Daemon not running" while per-daemon daemons are live — it errors-and-lists on many / acts-on-sole on one / idempotent "No daemons running" on none. The shared-default is why it won't drift again (ad-hoc-per-verb is how status got enumerate while stop got flat-fallthrough). `status` reuses `enumerateAdverts` (one enumerator) + gains `--name`; `unbind` gains per-daemon targeting (binding behavior unchanged — composes with the commit-2 delete-dir at the dir level). **AC-C1-6 GUARDRAIL:** AC-10/AC-12 isolation test files UNMODIFIED (git diff --quiet) + green. cli 89→98 (+9: selector 7, bare-stop 2), daemon 672 / ext 184 / shared 138 unchanged. build:clean+lint green. **CLI-only — no VSIX/redeploy, fire-and-run.** Sub-band: the recon's pre-mapping + the existing resolveCliTarget/statusEnumerate substrate meant low discovery; the selector composed them + the 5 conversions were uniform. **Commit 2 (list + delete-dir) remains.** |
| T-CLI-2 (commit 2) | `list` (read-only inventory) + `delete-dir` (safe operator-initiated pruning) + the delete-dir-class floor-deny (CLI + daemon-floor; closes the CLI multi-daemon conversion) | empirical 26–38 (mid ~32); legacy Medium-Large 32–55 | **≈16.6 min — MEASURED, hands-on** (t0=2026-06-10T02:29:26Z §0-stamped → commit %cI 2026-06-10T02:46:05Z; 16.6 min; no operator residual — CLI/floor-only) | **WELL SUB-BAND-LOW (vs empirical 26)** — the recon pre-mapped the config-dir layer (workspaces.json carries the identity; the floor pattern) and commit 1's substrate (the enumerate shape, selectDaemonTarget, the stop path) was reused, so the "safety stack" decomposed into small INJECTABLE pieces with near-zero discovery | **The CLI multi-daemon conversion CLOSER (commit 2 of 2).** **`list`** (`commands/list.ts` + `util/config-dirs.ts`): read-only inventory of the CONFIG-DIR layer (durable state that does NOT self-heal — adverts self-clean, config-dirs persist), enumerating `<getCliConfigDir()>/<16-hex hash>/` dirs, reading workspace+name from each dir's `workspaces.json` (config.json carries no identity; the trust store persists even when dead), liveness via a real HANDSHAKE (`ipcAddressForHash` + sendIpc, not file/pid presence — the anti-stale-trap). **`delete-dir`** — the only path that removes durable binding state, full safety stack: **(a)** bare LISTS (workspace+name) + requires a TYPED `--name` — **NO numbered pick**, always-name regardless of count, **no `--all`**; **(b)** live target → ALERT + require typing `confirm`/`cancel` (typing = friction); **(c)** confirm → graceful **stop THEN delete** (reuses commit 1's stopCommand — never yank a running daemon's dir); **(d)** deletion is the CLI's fs action against a DEAD dir — **daemon has NO delete capability** (structural self-protection). **THE security piece (AC-C2-6, named test):** the **floor** gains an explicit per-daemon-config-dir deny — `…/claude-bridge/<16-hex>` refs hard-denied under delegation regardless of root (catches the win32 `%APPDATA%\claude-bridge\<hash>` path the `~/.claude-bridge` tilde regex missed + a SIBLING daemon's dir the own-configDir check missed). **Composes with unbind** (AC-C2-7): dir-level vs token-level, neither duplicates. **AC-C2-8 GUARDRAIL:** AC-10/AC-12 isolation files UNMODIFIED (git diff --quiet) + green. cli 98→107 (+9: delete-dir 6, list 3), daemon 672→673 (+1 floor-deny), ext 184 / shared 138 unchanged. build:clean+lint green. **CLI/floor-only — no extension changes → no VSIX/redeploy, fire-and-run.** Sub-band: the safety stack was built as small injected functions (probe/readConfirm/gracefulStop/removeDir all injectable) so each AC is a clean unit test; the recon + commit-1 substrate left almost nothing to discover. **CLOSES T-CLI (both commits) — the CLI multi-daemon conversion is complete.** |
| T-CLI-3 | usability fix: render workspace+name in list/delete-dir (defect 1 — a test-vs-reality MISS) + numbered interactive pick for bare non-destructive verbs (defect 2 — un-built enhancement) (CLI-only) | empirical 18–26 (mid ~22); legacy Medium 22–35 | **≈11.4 min — MEASURED, hands-on** (t0=2026-06-10T03:02:23Z §0-stamped → commit %cI 2026-06-10T03:13:47Z; 11.4 min; no operator residual — CLI-only) | **WELL SUB-BAND-LOW (vs empirical 18)** — defect 1 was a ONE-FIELD fix (the §0(b) read found the exact bug) + test corrections; defect 2 was a single shared-selector branch | **Usability fix on the shipped CLI conversion — two defects of different character.** **Defect 1 (a genuine MISS that PASSED tests):** `list`/`delete-dir` rendered "(unknown workspace)" / "(unnamed)" — the render WAS correct, but `readIdentity` read `raw.workspaces` while the real WorkspacesStore persists `{version, entries:[{abs_path,name,…}]}`. **The §0(b) read found it by diffing my assumption against the REAL workspaces.json.** Why it passed: my test FIXTURES used the same wrong `{workspaces:[…]}` shape AND asserted the enumerated DATA, never the rendered surface against a real file — the exact data-not-surface gap. **Fix:** read `entries`; **corrected the tests to (a) use the real shape and (b) assert the RENDERED output** (`formatConfigDirList` contains the workspace+name, and NOT "(unknown workspace)"). **Verified on the REAL surface:** `claude-bridge list` now prints `clyde_claude_bridge  [live]  c:\Projects\clyde_claude_bridge` (and correctly flags a genuinely-orphaned config-dir with no workspaces.json as `(unnamed) [dead]` — a real prune candidate). **Defect 2 (un-built enhancement, postdated commit 1):** bare non-destructive verbs (stop/status/tail-log/token-rotate/tunnel-restart) now offer an **interactive numbered pick** when many daemons are live — act-on-sole when one; **non-TTY (piped/CI) → error-and-list** (the commit-1 behavior, no hang). Lives in the shared `selectDaemonTarget` many-branch (TTY-gated via `process.stdin.isTTY`, injectable for tests) so all five get it from ONE path (AC-C3-5). **`delete-dir` EXCLUDED (AC-C3-6, named test):** it doesn't call the selector — it keeps typed-name-no-number (a mis-picked number on an irreversible delete is the `REFINEMENT`-forbidden harm; the test asserts no `[N]` pick + names-visible-to-type). **AC-C3-7 GUARDRAIL:** AC-10/AC-12 isolation files UNMODIFIED (git diff --quiet) + green. cli 107→111 (+4: pick 2, list-render 1, delete-dir-guard 1; AC-C3-4 retitled an existing test), daemon 673 / ext 184 / shared 138 unchanged. build:clean+lint green. **CLI-only — no redeploy, fire-and-run.** The defect-1 lesson (a DISPLAY AC must be tested against the RENDERED SURFACE, not the data feeding it) is the headline; sub-band because the read pinpointed it. |
| T-CLI-4a | the systemic display-surface CLASS fix: ONE shared `renderDaemonList` + ONE config-dir enumeration for list/delete-dir/pick/error, orphan-prunable via --hash + mtime, surface-tested everywhere (CLI-only; commit A of 2, unbind binding-list = B) | empirical 28–40 (mid ~34); legacy Medium-Large 35–55 | **≈26.4 min — MEASURED, hands-on** (t0=2026-06-10T03:33:15Z §0-stamped → commit %cI 2026-06-10T03:59:41Z; 26.4 min; no operator residual — CLI-only) | **SUB-BAND-LOW (just under empirical 28)** — the recon pre-mapped the 4 renderers + 2 sources + the orphan reality, so the design was settled; the unification + test churn was mechanical once I committed to the config-dir source | **The systemic display-surface fix (recon root: RENDERING was never shared — 4 ad-hoc renderers from 2 sources, every verb its own chance to break).** Built ONE **`renderDaemonList`** (the rendering analog of selectDaemonTarget) + switched the selector's --name/bare paths from the ADVERT source to the unified **config-dir enumeration** (the same source `list` uses) so a daemon renders IDENTICALLY across list/delete-dir/pick/non-TTY-error (verified on REAL data: list + bare delete-dir print byte-identical lines). **Orphan dirs now actionable (AC-4a-3):** no-workspaces.json dirs render `(unnamed) [dead] hash <h> · last-modified <mtime>` and are prunable via **`delete-dir --hash`** — closing the unprunable-orphan gap (null name couldn't be --name-targeted; verified the real orphan 2d7c6310 now shows hash+mtime). **The still-live defect-1 gap CLOSED (AC-4a-5):** the pick's render (`defaultPickNumber`) was untested — the CLI-3 selector test injected `pickNumber` PAST the renderer; now `renderDaemonList({numbered})` (the exact surface defaultPickNumber writes) is asserted. **Non-TTY consistency (AC-4a-4):** `AmbiguousDaemonError` now renders name+workspace via the shared renderer (was names-only). **delete-dir safety preserved (AC-4a-6):** typed target (name OR hash), no number, live-target confirm, graceful-stop-then-delete, floor-deny — `--hash` weakens none. **Selector unification churn:** the 5 verbs' test-only `daemonsDir` → `configRoot`; selector.test + bare-stop test rewired from advert fixtures to config-dir subdirs + injectable `probe`; `enumerateAdverts` kept (status's bare enumerate still uses it). **AC-4a-7 GUARDRAIL:** AC-10/AC-12 isolation UNMODIFIED (git diff --quiet) + green. cli 111→117 (+6: renderDaemonList 4, ambiguous-render 1, --hash orphan 1), daemon 673 / ext 184 / shared 138 unchanged. build:clean+lint green. **CLI-only — no redeploy, fire-and-run.** Sub-band: the recon's pre-mapping collapsed the design; the unification was the real work but the config-dir substrate (CLI-2) + the shared renderer made it contained. **Commit B (unbind binding-list — the net-new second enumeration) remains.** |
| T-CLI-4c | `directories` command — print config-dir absolute paths (+ name/hash + live/dead) for verify-before-prune; separate from list, own spare formatter, reuses CLI-4a's enumeration (CLI-only) | empirical 12–20 (mid ~16); legacy Small-Medium 15–30 | **≈10.9 min — MEASURED, hands-on** (t0=2026-06-10T04:14:20Z §0-stamped → commit %cI 2026-06-10T04:25:15Z; 10.9 min; no operator residual — CLI-only) | **SUB-BAND-LOW (well under empirical 12)** — pure reuse of CLI-4a's unified enumeration + a 6-line spare formatter + registration; near-zero design | **The measure-twice companion to `delete-dir`.** A SEPARATE read-only `directories` verb printing each daemon's config-dir absolute PATH + minimal identity (name, or hash when unnamed) + live/dead — so the operator can `cd` there, eyeball the workspaces.json/logs, confirm it's the right dir, THEN `delete-dir`. **Kept separate from `list` (AC-4c-2):** `list` is the busy what's-running scan (name+workspace+live/dead+hash+mtime via the shared renderDaemonList); `directories` answers the distinct "WHERE on disk" with its OWN spare formatter — path+identity+liveness only, no workspace/mtime/block (a test asserts its output ≠ renderDaemonList's). **Shares CLI-4a's unified enumeration (AC-4c-4):** routes through `enumerateConfigDirs` (the SAME source list + the selector use, orphans included) — not a new/old separate one; read-only (handshake-for-liveness is the only daemon contact). **Orphan = the verify-before-prune story (AC-4c-3):** verified on REAL data — `2d7c6310c46c5916  [dead]  C:\…\claude-bridge\2d7c6310c46c5916` (hash stands in for the missing name + the path to go look) → then `delete-dir --hash` (CLI-4a). **AC-4c-5 GUARDRAIL:** AC-10/AC-12 isolation UNMODIFIED + green. cli 117→122 (+5: formatDirectories 4, command 1), daemon 673 / ext 184 / shared 138 unchanged. build:clean+lint green. **CLI-only — no redeploy, fire-and-run.** Sub-band (the sixth post-substrate run): once CLI-4a's enumeration existed this was a formatter + a registration — no enumeration, no mutation, no safety stack. **CLI-4b (unbind binding-list — the net-new daemon→bindings enumeration) is the last buildable CLI piece.** |
| T-CLI-4b | `unbind` binding-list — bare unbind LISTS the daemon's bindings (the daemon→bindings second enumeration) instead of erroring; typed-target-no-number revoke; CLI-only | empirical 18–28 (mid ~23); legacy Medium 22–35 | **≈14.4 min — MEASURED, hands-on** (t0=2026-06-10T04:28:47Z §0-stamped → commit %cI 2026-06-10T04:43:11Z; 14.4 min; no operator residual — CLI-only) | **SUB-BAND-LOW (well under empirical 18)** — the binding data already existed (`listBindings` on the status payload) + `status` already rendered it, so this was surface + wiring, not a new enumeration from scratch | **The LAST buildable CLI piece — the binding-layer sight.** `unbind` was NO-LIST (required you to already know the target id; the flow was "run `status` to find it, then `unbind <target>`"). Now **bare `unbind` LISTS** that daemon's bindings — workspace + client_id (both typeable targets) + issued/expires — so the operator can SEE what to revoke (the binding analog of `list`). **Reused the existing `status` IPC** (`oauth_bindings` is already on the payload) → **no daemon change, CLI-only** (no new IPC kind = no redeploy). **The footgun PRESERVED, reframed (AC-4b-5):** bare unbind was an error ("specify a target"); now it LISTS — and listing ≠ clearing, so "never a silent clear-all" still holds (the over-IPC test asserts bare calls `status`, NEVER `unbind_binding`). `UnbindTargetRequiredError` removed (dead). **Typed-target no-number (AC-4b-3, named test):** the list shows the ids to type; revoke needs a typed workspace/client id (or `--all`) — NO numbered pick (mirrors delete-dir: a mis-picked number revokes the wrong binding). **Daemon resolution via the CLI-4a selector (AC-4b-4):** verified on REAL data — bare `unbind` with 2 live daemons → error-and-list via CLI-4a's shared renderer (pick the daemon first, non-destructive), `unbind --name clyde_claude_bridge` → "No active bindings." (AC-4b-2 empty state, real). **AC-4b-1 surface-tested** against a real-SHAPED binding (no live bindings exist to use) + the over-IPC mock-status path. **Revoke mechanism + unbind/delete-dir compose unchanged** (token-level vs dir-level). **AC-4b-6 GUARDRAIL:** AC-10/AC-12 isolation UNMODIFIED + green. cli 122→125 (+3 net: +5 new [formatBindingList 3, over-IPC bare-list + empty 2], −2 removed obsolete bare-errors footgun tests), daemon 673 / ext 184 / shared 138 unchanged. build:clean+lint green. **CLI-only — no redeploy, fire-and-run.** **CLI display/enumeration surface now COMPLETE: daemon layer (list/directories/selector-pick) + binding layer (unbind-list).** Only the parked Bearer-token recon + domain-blocked stable-tunnel remain in the cluster. |
| T-BEARER-1 | remove the unconstrained Bearer auth path (OAuth-bound becomes the only model) — daemon + CLI + shared + tests + docs; security/isolation cleanup | empirical 26–40 (mid ~33); legacy Large 40–65 | **≈63.6 min — MEASURED, hands-on** (t0=2026-06-11T02:42:50Z §0-stamped → commit %cI 2026-06-11T03:46:24Z; 63.6 min; no operator residual — daemon/CLI, no redeploy) | **ABOVE empirical, near the TOP of legacy Large** — the recon's "low test cost / shallow entanglement" under-estimated the TEST breadth (~15 test files retargeted, not ~5), and TWO surfaced blockers round-tripped through the operator | **Removed the legacy unconstrained static Bearer — OAuth-bound is now the ONLY auth model.** `authenticate()` Bearer branch deleted (now 2-arg, OAuth-lookup-only; a non-bound credential → `invalid_token`, no `{kind:"unconstrained"}` grant). `WorkspaceBinding` collapsed to a single `bound` shape; the 3 enforcement sites (extension-router workspace-targeting, gate clamp, dispatch) now apply to EVERY connection — the 2 isolation bypasses are closed (the thesis-strengthening). `token rotate` removed entirely (CLI verb + `token_rotate` IPC kind + daemon handler); the `Token:`/`Bearer:`/`token_suffix` display surface removed; `generateToken`/`constantTimeEqual`/`config/token.ts` deleted (dead). **TWO in-flight blockers surfaced (both vindicated, both decided by the operator via a reissued dispatch — NOT a modal):** (1) **strict-schema config:** the schema is `.strict()` + `version:1` + no migration → removing `auth.token` would brick existing on-disk daemon configs test-INVISIBLY → DECISION: RETAIN `config.auth.token` optional/inert (new configs omit it; old configs validate; nothing reads it for auth). (2) **protected-isolation-test conflict:** `p3-acceptance.test.ts` (an AC-10/AC-12 file the convention says NEVER modify) *itself* tested the removed Bearer (the `:191` coexistence assertion + the old auth sig) → DECISION: the rule = invariant-preserved-not-byte-frozen; the authorized edits (delete the removed-feature test + mechanical sig fixes, 4 touches all invariant-preserving) STRENGTHEN the proof, touched loudly+minimally+authorized. **AC-B1 proofs:** AC-B1-1 named removal test ("a non-bound credential is rejected — no unconstrained path"); AC-B1-3 named bootstrap-independence ("OAuth bootstrap establishes a binding without the Bearer"); AC-B1-4 universal enforcement (enforceBoundWorkspace direct tests); AC-B1-2 bound path intact. **AC-B1-7 GUARDRAIL:** `get_open_editors.test.ts` UNMODIFIED (git diff --quiet); `p3-acceptance.test.ts` diff = exactly the authorized invariant-preserving touches (shown); bound coverage intact. **FLAGGED (report):** the `token_suffix` WIRE-field removal from the strict StatusPayload makes the new CLI reject a *still-running* old-binary daemon's status — but the live daemons are already DOWN (stale adverts) → restart on the new binary resolves it; self-healing (unlike the persistent config case). **Captured future-work:** a config-migration mechanism is the prerequisite for ANY field removal (every schema change hits the same test-invisible wall). daemon 673→665, cli 125→119, shared 138→135 (net deletes of unconstrained-accept/token-rotate cases minus the added AC-B1 tests), ext 184 UNCHANGED. build:clean+lint green. **daemon/CLI/shared/docs — NO extension change → no redeploy, fire-and-run.** Captures already filed (`used-and-should-exist` 09906b5, `document-superseded-but-live` ea85701) + the workflow memory `surface-blockers-as-text-stop-report` (surfacing was right twice; the AskUserQuestion modal was the wrong delivery — the operator reissues with the decision baked in). |
| T-CLI-5 | `unbind` ordering reshape — binding-presence-driven bare flow (fan-out bindings across daemons up front; present by where bindings ARE) — the LAST buildable CLI item (CLI-only) | empirical 16–26 (mid ~21); legacy Medium 20–32 | **≈13.2 min — MEASURED, hands-on** (t0=2026-06-12T02:12:11Z §0-stamped → commit %cI 2026-06-12T02:25:23Z; 13.2 min; no operator residual — CLI-only) | **SUB-BAND-LOW (well under empirical 16)** — reused CLI-4b's binding-list/fetch + CLI-4a's enumeration + the selector's pick pattern; the work was a reorder + a 3-way branch, near-zero net-new design | **The CLI surface CLOSER — fixed the operator's "pick a daemon → it's empty" bug.** Bare `unbind` resolved the daemon FIRST (menu → pick) then queried bindings — so with several daemons you could pick one to find "No active bindings." Reshaped to **fan out the binding query across all live daemons UP FRONT** (reusing the `status` IPC's `oauth_bindings` — **no daemon change, no new IPC kind, CLI-only**), then present by WHERE bindings are: **(AC-C5-1)** 0 anywhere → "No active bindings on any daemon", NO menu (the bug's fix — verified on REAL data); **(AC-C5-2)** exactly ONE daemon has them → skip the daemon-pick, list it directly; **(AC-C5-3)** SEVERAL have them → menu of ONLY the binding-bearing daemons (with binding counts; a test asserts a binding-less daemon is excluded). **Two-layer asymmetry preserved (AC-C5-5):** the DAEMON pick (when several bear bindings) is numbered/non-destructive; the BINDING revoke stays typed-target/no-number (mirrors delete-dir); non-TTY several-case → `AmbiguousBindingDaemonError` (error-and-list, no hang). **Targeted unchanged:** `--name`/`--workspace`/override skip the fan-out (the existing T-CLI-4b path; its 14 tests still green). **AC-C5-6 GUARDRAIL:** AC-10/AC-12 isolation UNMODIFIED + green. cli 119→123 (+4 fan-out tests), daemon 665 / ext 184 / shared 135 unchanged. build:clean+lint green. **CLI-only — no redeploy, fire-and-run.** Sub-band: pure reuse of the CLI-4a/4b substrate; the reshape is a reorder (fan-out before pick) + the 0/1/several branch. **THE LAST BUILDABLE CLI ITEM — the multi-daemon CLI surface is now COMPLETE** (daemon layer: list/directories/selector-pick; binding layer: unbind list + binding-presence-driven flow). Remaining in the cluster: only the domain-blocked stable-tunnel + the autonomous-loop layer + the config-migration future-work. |
| CB-TOOLTIP-PID-FIX | small extension display-wiring fix (diagnosis → no code break; coverage gap closed) | (none stated; analog: small display fix + test ~8-15 min) | **≈10 min — MEASURED** (epoch 1780767251 dispatch-receipt → 1780767876; 625s = 10.4 min) | in-band (vs ~8-15 analog) | **Outcome: NO code break — the 3-point chain (hello_ok sends → IpcClient captures → tooltip renders) is correct at HEAD, and the built `dist/extension.js` contains it (6 refs, positive-confirmed not inferred).** Diagnosis: #1 already proven on the wire by the CB-DAEMON-LIFECYCLE-FIX server test; #2 (IpcClient.getDaemonPid capture) and #3 (tooltip via getDaemonPid, the production path — getDaemonInfo is the undefined stub) were UNTESTED — that coverage gap is what allowed the uncertainty, so I closed it: +3 ipc-client capture tests (present/absent/cleared-on-disconnect) + 2 status-bar tooltip tests (pid shown via getDaemonPid with getDaemonInfo undefined; omitted cleanly when null). The operator's "connected, no pid" symptom is the DEPLOY COROLLARY — the long-lived VS Code extension host was running a pre-fix / un-reloaded build (the CLI `status` works because it's re-invoked fresh each call). Resolution = rebuild + **reload the VS Code window**. No src change (would be fabricating a fix for correct code — the anti-fabrication discipline). Extension 182→187 (+5); full suite 954 pass + 17 skip; build:clean + lint green. |

## P1

| Task | Phase | Predicted bucket | Predicted range | Actual | Variance vs midpoint | Notes |
|---|---|---|---|---|---|---|
| T-P1-001 | Phase 1 — shared types | Small | 30-60 min | 0:07 | -84% | Came in well under band. Five files: 3 new schema modules + config extension + index re-exports. 50 new tests. Established Zod-schema + test pattern from P0 carried over cleanly; no discovery surface. Confirms a "Small-consolidation" sub-bucket may exist — Small tasks that purely apply existing patterns without new design surface run closer to trivial speeds (parallel to v0.4's "Medium-consolidation" finding from T-0020). Flag for repeat-observation before codifying. |
| T-P1-001.5 | Infrastructure (insert) | Small | 30-60 min | 0:05 | -89% | Doc placement + git push to github.com. Bucket revised upward from original Trivial+setup after T-P1-001 verdict added file-placement scope. Came in at trivial-bucket speed (5 min) because the deposit files were mostly already at canonical paths — only one rename (snapshot `-amended` → `snapshot/`), one overwrite (build-plan amended content → canonical filename), and one duplicate delete needed. Doc-only; no async-discipline streak impact. Bundles T-P1-001's uncommitted source work per §14.7 user-decision. Second "Small-but-actually-trivial" data point this session — repeat-observation supporting a "Small-consolidation" sub-bucket. |
| T-P1-001.6 | Infrastructure (insert) | Small | 30-60 min | 0:06 | -87% | README factual correction (4 deltas: status paragraph, gate-status table P0+P1 rows, git-clone URL, + Repository line added) + `gh` install via winget + `gh auth login --web` device-code flow + AC-6 mechanical close. User interaction: browser device-code paste at 157C-0284. Three consecutive "Small-but-trivial" data points (T-P1-001, T-P1-001.5, T-P1-001.6) all at -84%/-89%/-87%. Strong evidence for a "Small-consolidation" sub-bucket; would propose for methodology v0.5. Doc-only; no async-discipline streak impact. |
| T-P1-002 | Phase 2 — registry stub + validation | Small | 30-60 min | 0:06 | -87% | First code task at this pace. Three files: registry.ts (45 lines), workspace/config.ts (66 lines), main.ts wiring (4 lines net). 14 new tests (9 registry + 5 config; 2 symlink tests platform-skipped on Windows). 19th consecutive zero-fire on async-discipline rules (no async in this task; streak preserved). **Important finding: the "Small-but-trivial" pattern is NOT doc-only** — fourth consecutive landing, first one was code with mild filesystem-validation discovery. v0.5 sub-bucket proposal strengthens. Next datapoint to watch: Phase 3 (Medium-fresh, 60-120 min predicted) — does Medium-fresh land in band, or does it also compress? |
| T-P1-003 | Phase 3 — job queue + state machine | **Medium-fresh** | 60-120 min | 0:14 | -84% | **Calibration-decisive: Medium-fresh ALSO lands sub-band.** Substantial task: 4 new files in jobs/, DailyTimer extraction from T-0007's AuditLog (refactor + new util), state machine with 5 mark methods + claimNext atomicity + terminal-promise primitive + fake-clock retention sweep. Real §22.5 consultation interrupt (sweep wiring decision). 43 new daemon tests (7 daily-timer + 2 id + 34 queue). 1 reactive lint fire (22× redundant `!` non-null assertions in queue.test.ts; eslint --fix resolved cleanly — vitest's `expect(x).not.toBeNull()` provides narrowing assertions that strip the need for `!`). 20th consecutive zero-fire on async-discipline rules. **Conclusion: the calibration bands aren't bounded to "consolidation" tasks — Medium-fresh with genuine design surface + cross-cutting refactor + consultation still compresses 5-10×.** Two interpretations: (a) the bands set in P0 reflected per-prompt overhead that scope-pre-conversation has now eliminated, OR (b) the cached context from continuous-session work compounds further than expected. v0.5 should reconcile this — current bands are systematically pessimistic. |
| T-P1-004 | Phase 4 — tool surface + StubJobRunner | empirical 15-25 min / legacy Small-medium 30-90 min | dual-band | 0:17 | **in empirical band (mid)** / -72% vs legacy midpoint | **First task with empirical prediction — landed IN BAND.** Empirical recalibration (per T-P1-003 finding) confirmed: tasks with moderate surface + resolved scope land 15-25 min. Legacy Small-medium remains systematically pessimistic. Substantial scope: 4 new files (delegate.ts, poll.ts, cancel.ts, runner.ts) + audit_metadata side-channel refactor on dispatch.ts + ToolHandlerError typed-error class + AuditEntry schema extension + main.ts wiring. 31 new daemon tests across 4 files (delegate 18, poll 6, cancel 4, runner 6). 1 reactive lint fire (unused `_job_id` underscore param on JobRunner interface impl; inline-disabled), 1 reactive fix on test-shape (handlers needed `async` so synchronous `throw ToolHandlerError` becomes a rejected promise for vitest `.rejects` assertions — non-async functions throw synchronously before expect sees a Promise). 21st consecutive zero-fire on async-discipline rules. Empirical bands hold; v0.5 can codify these alongside legacy reference. |
| T-P1-005 | Phase 5 — acceptance harness skeleton | empirical 25-45 min / legacy Medium-fresh 60-120 min | dual-band | 0:16 | **just below empirical band low end (-54%)** / -82% vs legacy midpoint | Second datapoint with dual-band predictions. Came in just under the empirical low end. Substantial scope: 4 new script files (mcp-delegate-client.mjs, acceptance-p1.mjs ~530 lines, acceptance-p1.ps1, acceptance-p1.sh stub), stub_behavior schema in shared, --allow-stub-config flag scan + gating in daemon main.ts, 7 new schema tests, main.test.ts updated (latent T-P1-003 gap fixed: dailyTimer added to shutdown order assertion). **First harness run found 3 real bugs: AC-2 assertion encoded design-doc semantics that disagreed with implementation reality (queued_position 0 vs 1+); AC-4 + AC-13 hit cumulative-queue-depth blowing wait_ms budgets due to single-concurrent runner across sequential ACs in same group.** Fixed by loosening AC-2 to impl semantics + adding `drainJobs()` helper between ACs + lowering stub delay_ms from 3000→1500. Re-run: 9/9 PASS. AC-4 evidence: poll resolved at 1516ms (stub delay 1500, wait 6000 → terminal_promise fired within 16ms of stub completion, well before wait timeout). 22nd consecutive zero-fire on async-discipline rules. **Empirical band needs refinement**: tasks involving live runs (debug-fix-rerun cycles) may need a higher midpoint. The 25-45 lower bound felt right for the planning-and-writing-and-first-run phase; the actual surprise was that debug-and-fix cycle was also tight (5 min). Recommend dual-band reporting continue; revisit after Phase 6-9 data. |
| T-P1-006 | Phase 6 — transcript writer + orphan handling | empirical 15-25 min / legacy Small 30-60 min | dual-band | 0:08 | **below empirical band low end (-60%)** / -82% vs legacy midpoint | Third dual-band datapoint. Came in well under both bands. Pure code task (no live cycle): 2 new src files (transcript.ts ~110 lines, transcript-orphan.ts ~100 lines), main.ts wiring (~25 lines), 2 new test files (transcript 16 tests, transcript-orphan 9 tests). 2 platform-skipped tests on Windows (file-mode 0600/0700 verification). Daemon now 234 passing + 8 platform-skipped across 27 files. 3 reactive fixes: (a) 6× JSON.parse `no-unsafe-*` lint fires — typed via `as { type, reason, ... }` casts at the parse site; (b) "mkdir -p" test pre-assertion incorrect (existsSync of unrelated dirname returned true) — removed pre-assertion, restructured to assert post-construction state; (c) cross-platform transcriptPath test hardcoded forward-slash input — used `path.join` to build the input so platform separators apply uniformly. 23rd consecutive zero-fire on async-discipline rules. **Pure-code Small task with no live cycle landed even faster than live-cycle T-P1-005.** Two interpretations: (a) the live-cycle predictions overestimated debug overhead; (b) per-task overhead is now bounded by orchestrator scope-resolution + cached context, regardless of live vs unit. Empirical band may need tightening for "pure-code Small" sub-bucket; legacy bands remain consistently 3-7× pessimistic. |
| T-P1-007 | Phase 7 — snapshot + diff computation | empirical 25-40 min / legacy Medium-fresh 60-120 min | dual-band | 0:08 | **below empirical band low end (-76%)** / -91% vs legacy midpoint | Fourth dual-band datapoint. Came in tied with T-P1-006 despite larger scope: 2 new src files (snapshot.ts ~235 lines with git CLI + ignore-package walker + binary detect + streaming sha256; diff.ts ~190 lines with git diff / fallback paths + chunked path args + LF normalization), 2 new test files (snapshot 11 tests including git-conditional describe.skipIf; diff 7 tests), 2 net-new runtime deps (ignore@^5.3.2, diff@^9.0.0). Daemon now 252 passing + 8 platform-skipped across 29 files. Real §22.5 consultation interrupt mid-task on the diff-package question (user kept it; documented). 2 reactive fixes: (a) `ignore` package's TypeScript namespace import not callable under project's ESM-interop config — wrapped with `.default ?? factory` indirection at import site; (b) eslint flagged 2 unused-disable directives for naming-convention rule (not enabled) — removed disables. 24th consecutive zero-fire on async-discipline rules. **Both new deps installed cleanly (0 vulnerabilities); dep additions felt lightweight rather than heavy.** Sustained sub-band pattern reinforced across 4 datapoints; recommend v0.5 codify empirical bands as primary, legacy as reference annotation. Net interpretation: with scope-pre-resolved + cached context + established patterns, task time is dominated by typing speed + occasional micro-debug cycles, not by integration overhead. |
| T-P1-008 | Phase 8 — report assembler + cross-platform-test-inputs pattern doc | empirical 10-20 min / legacy Small-medium 60-90 min | dual-band | 0:06 | **below empirical band low end (-60%)** / -92% vs legacy midpoint | Fifth dual-band datapoint — fastest yet despite "pure-code-with-discovery" classification. Pre-task hypothesis: lands closer to T-P1-003 (14 min, with discovery) than T-P1-006 (8 min, no discovery). Actual: 6 min, faster than either. Scope: 1 new src file (report.ts ~280 lines, fail-soft JSONL parsing + summary backward-walk + bash command + exit-code regex extraction + 4-tier truncation precedence), 1 new test file (report.test.ts ~410 lines, 24 tests across parseTranscript shapes + shell extraction + fail-soft + summary cap + assembleReport integration + truncation precedence), 1 new project pattern doc (`cross-platform-test-inputs.md` at 5th use site per dispatch). Daemon now 276 passing + 8 platform-skipped across 30 files. Zero reactive deviations — first task this session with no lint or test fixups needed. 25th consecutive zero-fire on async-discipline rules. **Pattern doc creation took ~1 min** (followed test-token-fixtures.md template structure). The "discovery surface" (transcript shape commitments) was substantial on paper but resolved in code via Decision 1's documented assumption + Decision 4's fail-soft fallback — both moved the runtime risk to Phase 9 (where the real SDK lands) instead of demanding speculative defenses now. Across 5 datapoints, sub-band pattern is now too consistent to attribute to noise; v0.5 recalibration with empirical-as-primary is the right move. |
| T-P1-009 | Phase 9 — Claude Agent SDK integration + cancellation | empirical 30-50 min / legacy Large 90-180 min | dual-band | 0:18 + ~5 min reshape | **below empirical band low end (-55%)** / -87% vs legacy midpoint | Sixth dual-band datapoint. Largest P1 task by surface: sdk-runner.ts ~310 lines (query() AsyncGenerator iteration, transcript pass-through, canUseTool deny patterns, error classification, AbortController cancellation); sdk-runner.test.ts 5 SMOKE tests gated on ANTHROPIC_API_KEY — all 5 skipped this run (key unset in shell env); modifications to report.ts (effectiveContent helper reads SDK's nested .message.content while preserving legacy flat shape), main.ts (SdkJobRunner default; StubJobRunner retained under --allow-stub-config; ANTHROPIC_API_KEY-absent warning), report.test.ts (4 new SDK-shape tests). Daemon now 280 passing + 13 skipped (5 SMOKE + 8 platform-skip) across 31 files. **First real external runtime dependency that executes**: `@anthropic-ai/claude-agent-sdk@^0.3.150` (renamed from claude-code). **Reactive deviation from Decision 4**: SDK d.ts says query.interrupt() is "only supported when streaming input/output is used"; for single-prompt delegations AbortController is correct path (and no .close() exists on Query). Documented in sdk-runner.ts header. 3 lint fires resolved at fix-site (unused computeDiff import; unused _options param; base-to-string on input.command). 26th consecutive zero-fire on async-discipline rules. **Significant work in this task was SDK type-system spelunking** (reading sdk.d.ts via grep+awk to find query/Options/CanUseTool/PermissionMode/SDKMessage exports + the AbortController vs interrupt() discovery). Pattern: type-first SDK integration via direct d.ts inspection works well at this scale. **Post-commit orchestrator scope reshape (~5 min)**: T-P1-009 originally added a 32KB prompt cap at delegate.ts handler level + a unit test (commit 12120e4). Orchestrator decided post-commit that the 32KB figure was speculative (no SDK/API/empirical basis); reverted the cap + test in a follow-up commit; replaced with a deferral comment at the handler site; added the design-doc 32KB-removal to P1-close doc-debt; updated the acceptance-p1.mjs AC-15 wording from "deferred" to "by design". Same shape as the T-P1-007 mid-task `diff`-package consultation, but post-commit rather than mid-task. |
| T-P1-010 | Phase 10 — cross-platform live SMOKE validation (Win + WSL Ubuntu) | empirical 25-45 min / legacy Medium-fresh 60-120 min | dual-band | 0:24 | **in empirical band (low-mid)** / -73% vs legacy midpoint | Seventh dual-band datapoint — first one this session to land **inside** an empirical band rather than below it. The extra time landed on a reactive-source-fix cycle (see below), not on planning or typing speed. Scope: ran the 5 SMOKE tests in `sdk-runner.test.ts` on Windows host + WSL Ubuntu host with a live ANTHROPIC_API_KEY (session-scoped, env-only). WSL-side prep included a clean `node_modules` reinstall after the prior worktree's incremental install left `@modelcontextprotocol/sdk` `.d.ts.map` files orphaned from their `.d.ts` siblings (broke `tsc -b`). **Reactive source fix mid-task** (in-scope per SMOKE-uncovered defect rule): Windows smoke #2 failed on first pass — Claude in `permissionMode: "plan"` called `ExitPlanMode`, which the SDK auto-approves and flips permissionMode to `default`; on the next turn the model would have written the file. Discovered via a one-off `.smoke-debug/debug-readonly.mjs` repro that preserved the transcript dir (the test's afterEach() wiped it). Fix: added `READ_ONLY_DISALLOWED_TOOLS = ["Write","Edit","MultiEdit","NotebookEdit","ExitPlanMode"]` and set `options.disallowedTools` when `mode === "read_only"`, so writes are blocked at the SDK layer regardless of any permissionMode flip. Test #2 assertion rewritten from `(complete && !exists) || (failed && permission)` to the semantic contract `status !== "cancelled" && !existsSync(bad.txt)`. Final SMOKE table: Windows 5/5 in 80.78s (smoke#1=13.0s, #2=37.5s, #3=10.9s, #4=6.5s, #5=11.2s); WSL 5/5 in 67.20s (#1=9.1s, #2=31.7s, #3=11.7s, #4=6.2s, #5=7.0s). Daemon full-suite green: 280 pass + 13 skip (5 SMOKE + 8 platform). 27th consecutive zero-fire on async-discipline rules. **Calibration note:** live cross-platform validation has more wall-clock overhead than pure-code phases (WSL rsync + install + build + parallel SMOKE runs), but the actual Clyde-time stays bounded because work parallelizes across background tasks. The empirical-band low end (25 min) was right; tasks that include real-runtime debug-fix-rerun cycles consume the band's headroom. |
| T-P1-011 | Phase 11 — acceptance harness MCP-path SMOKE (AC-5/6/8) | empirical 20-30 min / legacy Medium-fresh 60-120 min | dual-band | 0:30 | **at empirical band high edge** (vs low: +50%; vs mid 25: +20%; vs high: 0%) / -67% vs legacy midpoint | Eighth dual-band datapoint — second live-API task (after T-P1-010). Scope: extract shared lib (`scripts/lib/harness-common.mjs` ~190 lines) covering temp-env, writeConfig, daemon spawn/stop, and small pass/fail/extractResult helpers; refactor `scripts/acceptance-p1.mjs` (T-P1-005) to import from it (verified still 9/9 PASS post-refactor); write new `scripts/acceptance-p1-smoke.mjs` (~230 lines) covering AC-5/6/8 via the MCP wire against the real SDK; write `.ps1` wrapper + `.sh` stub for Phase 12. Live SMOKE result table: AC-5 PASS 10.2s, AC-6 PASS 37.8s, AC-8 PASS 6.3s wall (cancel→terminal 2.2s of 15s budget). **Reactive harness-brittleness fix mid-task** (in-band per SMOKE-uncovered defect rule): first run reported AC-6 PASS in 38ms with `status=undefined` — too fast to be real. Diagnostic one-shot revealed `poll_delegation` was rejecting our `wait_ms: 90000` at the `PollInputSchema` boundary (schema caps at 60000), and the error envelope `{ isError: true, content: [{type:"text",text:"invalid input: ..."}] }` slipped past the assertion (`p.status === "cancelled"` was `undefined === "cancelled"` → false → "pass"). Two-part fix: lowered `AC6_WAIT_MS` to the schema's 60000 cap; added an `unwrapOrThrow(callResult, where)` helper that hard-fails the AC when `isError === true`, and replaced every `extractResult(await callTool(...))` in the SMOKE harness with `unwrapOrThrow(...)`. Schema-cap discovery and the fix took ~7 min of the 30-min total. Daemon source unchanged (no production code touched). Lint clean across 3 workspaces; daemon full suite 280 pass + 13 skip (unchanged). 28th consecutive zero-fire on async-discipline rules. **Calibration:** the empirical-band high-edge landing matches the dispatch's stated prediction reasoning ("if T-P1-011 lands in-band, the estimate was right"). The 7-min reactive cycle is consistent with T-P1-010's 5-min reshape — live-runtime tasks reliably consume ~5-10 min of band headroom when a real defect surfaces, vs zero headroom for pure-code phases. v0.5 sub-bucket signal strengthens: "live-runtime tasks" sit at empirical 20-45 min depending on count of debug cycles; "pure-code tasks" sit at 5-15 min regardless of stated complexity. **Shared-lib extraction observation:** the call was unambiguous (~190 lines >> 50-line trigger); refactor of T-P1-005's harness was mechanical (one-line replacements at function-call sites) with no semantic changes, verified by re-running the harness and getting the same 9/9 PASS result. Worth recording as a pattern: **mechanical-replacement refactors of harness code are safe to bundle with related new work IF re-running the prior harness is cheap.** Re-run took ~5s of test time. |
| T-P1-012 | Phase 12 — cross-platform WSL Ubuntu MCP-path validation | empirical 15-25 min / legacy Trivial+setup 10-25 min | dual-band | 0:20 | **in empirical band (mid)** (vs low: +33%; vs mid 20: 0%; vs high: −20%) / **in legacy band (mid-high)** (vs low: +100%; vs mid 17.5: +14%; vs high: −20%) | Ninth dual-band datapoint — third live-API task. **First task where empirical and legacy bands largely overlap** (15-25 vs 10-25); the empirical adjustment was for scope-pre-resolved + cached-context work that DOES apply here (purely mechanical execution + two small platform fixes). Scope: WSL prep (rsync from /mnt/c, clean `node_modules`, install, build); run T-P1-005 stub harness (`bash scripts/acceptance-p1.sh`); run T-P1-011 SMOKE harness (`bash scripts/acceptance-p1-smoke.sh` with ANTHROPIC_API_KEY via WSLENV); reconcile vs Windows; doc updates. **Two reactive platform fixes** within dispatch's <20-line budget each: (1) Linux/darwin branch in `ensureCloudflaredOnPath` checking `~/cloudflared`, `/usr/local/bin/cloudflared`, `/usr/bin/cloudflared` — needed because daemon's `await tunnelManager.start()` gates `ready\n` and missing-cloudflared spawn fails before ready; +20 lines (within budget). (2) Lazy `await import("undici")` in `mcp-delegate-client.mjs` — undici@^8.3.0 (root devDep) needs Node ≥ 22.19 and crashes at module load with `webidl.util.markAsUncloneable is not a function` on WSL's user-local Node 20.18; DNS workaround is only load-bearing for trycloudflare URLs (T-0019) so localhost-only harnesses degrade gracefully with a stderr warning; +17 lines (within budget). **WSL stub harness:** 9/9 PASS in ~7s (AC-1 56ms; AC-3 10ms; AC-4 1513ms event-driven via terminalPromise; AC-10 21/27 audit entries carry job_id+workspace_id). **WSL SMOKE harness:** AC-5 PASS 9.3s; AC-6 PASS 6.7s (status=complete; Windows took 37.8s — Claude's path selection in plan+disallowedTools is non-deterministic); AC-8 PASS 5.4s wall, cancel→terminal 1.4s (Windows 2.2s; both well under 15s budget). Total wall time per harness on WSL was much less than on Windows because the cumulative SDK call latency was lower this run; cross-platform variance is normal. Windows post-fix spot-check: T-P1-005 harness still 9/9 PASS; the lazy-undici-load is a no-op on Windows since undici 8.3.0 loads cleanly there. Daemon source unchanged across this task; lint + 280 pass + 13 skip preserved. 29th consecutive zero-fire on async-discipline rules. **Calibration:** the prediction reasoning ("floor ~12 min if nothing surfaces; ceiling 25 if both platform fixes are needed") held — both platform fixes WERE needed, and the task landed near the predicted ceiling. The Linux-cloudflared issue was anticipated in the dispatch (named explicitly). The undici/Node-engine issue was flagged earlier in T-P1-010 as a known WSL artifact but its impact on the MCP client wasn't visible until the harness actually loaded the client. **v0.5 signal:** the empirical/legacy overlap converging is meaningful — when scope is purely "execute existing work + make small platform adjustments," there's nothing for the scope-pre-resolution-bonus to compress; the two bands describe the same task. Empirical-vs-legacy divergence is specifically a function of how much pre-resolved-design + cached-context the task carries. |
| T-P2-015 | P2 Phase 15 — gate close + doc-debt sweep + P3 handoff | empirical 20-35 min / legacy 20-45 min | dual-band | ≈0:30 (30 min wall-clock estimate; mostly continuous; brief operator-clock pauses between rounds but no extended interrupt) | **lower-half** vs empirical (vs low 20: +50%; vs mid 27.5: +9%; vs high 35: −14%) / **lower-half** vs legacy (vs low 20: +50%; vs mid 32.5: −8%; vs high 45: −33%) | Gate-close + sweep + handoff doc; doc-only (zero source/test changes). **Section A doc-debt sweep:** 5 surgical edits to project-state.md (C-23 status flip; C-27 row formalization; v0.7 candidates M-G..M-O replacement; OAuth framing fix) + 2 to README (GATE-CLOSED marker on phase-table row + snapshot pointer under section header). **Section B snapshot drafting** (~280 lines, 15 sections per spec): the bulk of work. Used P1-close as structural template; populated with P2-specific content from project-state/milestones/calibration-log/design-doc. **Section C tracking docs:** milestones P2-row flip + T-P2-015 entry + this row. **C-13 finding worth recording:** C-27 was referenced across 4 docs as "see C-27 in project-state" but never had a row in the v0.6 candidates table — formalized as part of the sweep. Hard-stop didn't fire because the sweep's job is to fix exactly this kind of doc-debt; treated as in-scope discovery, not implementation drift. **Workflow tool NOT used:** Section A's edits were mostly interdependent (all to project-state.md) so parallel dispatch would have added serialization overhead; Section B was a single coherent document (non-parallelizable per dispatch). M-L (parallel-sub-agent leverage) stays at 2 instances; T-P2-015 doesn't add a 3rd because the surface didn't fit. **C-25.1 fresh-lint:** clean. **C-25.2 N/A** (doc-only; no .vsix change). **C-25.3 N/A** (doc-only; no runtime surface affected). **Calibration shape:** lower-half landing matches dispatch's prediction reasoning ("B.9 OAuth prep + B.4 AC table are content-dense"); the AC-table assembly was the densest part but the existing project-state task entries provided most of the per-AC evidence pointers, reducing reconstruction cost. **53rd consecutive zero-fire on async-discipline rules.** |
| T-P2-014 | P2 Phase 14 — v0.6 methodology codification | empirical 25-40 min / legacy 25-55 min | dual-band | ≈0:35 (35 min wall-clock estimate; imprecision noted — task spanned a user interrupt for scope-lock file drop + multiple "continue" round-trips between conversation rounds; clock not paused for those gaps but they fragmented attention) | **upper-half** vs empirical (vs low 25: +40%; vs mid 32.5: +8%; vs high 40: −13%) / **lower-half** vs legacy (vs low 25: +40%; vs mid 40: −13%; vs high 55: −36%) | Methodology codification per scope-lock 2026-05-28. **Heavy doc drafting** (~570 lines of v0.6 doc preserving v0.5 substance verbatim where unchanged, with explicit revisions in §3.5/§5.2/§8.2, three new sections §10/§11/§15, plus repurposed §12 and new §14). **Workflow tool used for Section B cross-reference updates** — 3 parallel sub-agents, one per file (README, p2-build-plan, pattern doc); 44s wall-clock; 8 edits applied + 4 historical references intentionally preserved with rationale. **C-13 grep, no hard-stops:** scope-lock candidates fully align with project-state v0.6 candidates table; v0.5 structure matches scope-lock assumption (12 sections + Preamble); 22 P2 datapoints in calibration log per scope-lock A.6 expected; 9 pattern docs per scope-lock A.5. Minor C-23 status drift flagged (project-state line 53 still "open" while scope-lock D says closed-by-T-P2-007). **C-22 text recovery (per scope-lock E TBD):** present in project-state line 45 as fully recovered; deferred to v0.7 per scope-lock E. **Differential-review statement** present in v0.6 §15 first subsection per scope-lock A.3. **Empirical band table refresh** organized by task-shape (12 rows) rather than by phase, with 12 new shape rows added covering bounded-fix, medium pre-resolved, large multi-subsystem, doc rewrite, methodology codification, and diagnostic-add shapes. **Streak counter stays out of methodology** per scope-lock A.7. Doc-only — `git diff --stat packages/ scripts/` empty. Tests 685+15 unchanged. Lint clean (C-25.1 fresh-verified). C-25.2 + C-25.3 N/A (doc-only). **Workflow leverage observation:** the 3 cross-reference updates ran in parallel via Workflow tool in 44s; sequential would have been ~3-5 min. Net savings ~2-4 min on this small Section B; the larger leverage came from v0.6's single-doc draft being done in the main loop (correctly NOT parallelized per scope-lock — a coherent doc cannot be sliced across agents). M-L (parallel-sub-agent leverage for independent-file edits) now has 2 confirmed instances (T-P2-013 + T-P2-014). **52nd consecutive zero-fire on async-discipline rules** (tracked in project-state, not methodology per scope-lock A.7). |
| T-P2-013 | P2 Phase 13 — runbook + walkthrough + overview updates; OAuth narrative correction | empirical 20-30 min / legacy 15-35 min | dual-band | 0:18 (18 min wall-clock dispatch-to-push; workflow-parallelized) | **lower-half** vs empirical (vs low 20: −10%; vs mid 25: −28%; vs high 30: −40%) / **mid** vs legacy (vs low 15: +20%; vs mid 25: −28%; vs high 35: −49%) | **First T-P2 dispatch that used the Workflow tool.** 7 doc sections (A-G) dispatched as parallel sub-agents (one agent per file); 186s of workflow wall-clock with 7 concurrent writes; the rest is C-13 grep + verification + tracking-doc edits + commit/push in the main loop. Doc-only — zero source/test changes. **C-13 mismatch flagged** (no hard-stop): dispatch's Section D claimed AC-P2-5 contained the 403 workspace_untrusted text, but actual file's AC-P2-5 at line 197 is the lifecycle text; the workspace_untrusted narrative lives in the Reserved-for-P3 note at line 208 — landed the spec-D rewrite there. **One reactive at-site fix during link verification:** Section A's walkthrough rewrite deleted the former `## P1 — Delegation surface` section, breaking a `README.md:96` anchor link; fixed with a generic walkthrough link. **C-25.2 N/A** (no .vsix change). **C-25.3 N/A** (no runtime surface affected). Test totals 685+15 unchanged. Lint clean. **Workflow effectiveness:** the 7-section parallel write completed in 186s; sequential equivalent would have been ~10-15 min of file reads + writes + diff checks. Net session-time savings ~6-8 min. Pattern worth recording for future doc-sweep tasks with independent file surfaces. **51st consecutive zero-fire on async-discipline rules.** |
| T-P2-012 | P2 Phase 12 — cross-platform validation (Windows + WSL Ubuntu) | empirical 15-25 min / legacy 15-35 min | dual-band | 0:10 (10 min; 20:42:04 → 20:52 wall-clock dispatch-to-push) | **sub-band-low** vs empirical (vs low 15: −33%; vs mid 20: −50%; vs high 25: −60%) / **sub-band-low** vs legacy (vs low 15: −33%; vs mid 25: −60%; vs high 35: −71%) | Pure execution-validation task — no source-code changes; ran the T-P2-011 harness on both platforms. **WSL harness: 10/10 PASS on first run, no platform fixes needed.** Pre-flight C-13 grep #2 caught one operator-environment quirk: Node v20.18.0 lives at `~/node-v20/bin/node` (portable install) and is not on default WSL PATH; added `scripts/run-p2-wsl.sh` (~10 lines) as a launcher that exports the PATH before invoking the harness. Same pattern T-P1-012 used for cloudflared. **Windows re-run: 10/10 PASS on second try** (first try got TIME_WAIT on 7423; 5s wait resolved). Elapsed-time parity within 2x across all 10 ACs. **C-25.2 cross-platform via `scripts/verify-vsix-wsl.sh` (~50 lines)**: `python3 -m zipfile -e` (avoids apt-installing `unzip`) → `dist/extension.js` exists, `package.json` valid, `grep -c "@claude-bridge/" → 0`. Two helper scripts kept as operator artifacts in `scripts/` so future cross-platform runs don't re-derive the PATH dance. **Closes AC-P2-15** (cross-platform parity). No new v0.6 candidates surfaced — the WSL Node PATH quirk is operator-environment-specific. **50th consecutive zero-fire on async-discipline rules.** |
| T-P2-011 | P2 Phase 11 — acceptance harness via mock VS Code extension | empirical 25-35 min / legacy 25-55 min | dual-band | 0:25 (25 min; 20:09:46 → ~20:35 wall-clock dispatch-to-push) | **lower-half** vs empirical (vs low 25: 0%; vs mid 30: −17%; vs high 35: −29%) / **lower-half** vs legacy (vs low 25: 0%; vs mid 40: −38%; vs high 55: −55%) | Test-infrastructure-only task: 2 new scripts (`scripts/mock-extension.mjs` ~270 lines pluggable IPC mock + `scripts/acceptance-p2.mjs` ~580 lines with setup/teardown + 10 AC tests). **C-13 grep #1 finding:** `scripts/lib/harness-common.mjs` already exists (T-P1-011); reused for daemon-lifecycle, NOT duplicated. Scope did NOT shrink — the mock-extension surface is genuinely new; harness-common only covers daemon spawn/stop/config. **Spec deltas (noted in report):** AC-P2-9 timeout is hardcoded 5min in pending.ts; harness tests the disconnect-cancel pathway as a proxy (same intent: approvals don't hang). AC-P2-5 untrusted/unregistered collapse at daemon level; differentiated via mock call-log inspection. **Two reactive cycles in run:** (1) AC-P2-10 floor 5ms→2ms (localhost-mock denial measured 4ms); (2) AC-P2-14 ambiguous regex widened to match message body, not just reason code. Both at-site, sub-2min each. **Operator coordination:** Windows IPC pipe system-wide — stopped user daemon before run, will restart after. **v0.5 §7 brittleness defenses all applied** (unwrapOrThrow, both-sides asserts, elapsed-time floors, fresh sandbox per test). No source-code changes; existing 685+16 unchanged. Lint clean. C-25.b N/A. **C-35 actual: 25 min (lower-half empirical; bottom edge of band).** Drivers: (1) reactive cycles were fast (sub-2-min each) because elapsed-floor tuning was a one-line lower-bound adjustment and the ambiguous-error regex was a one-line widen; (2) two genuinely-new substantial scripts (~850 total lines) but the daemon-IPC framing was a direct mirror of the existing extension's IpcClient pattern, no API surprises. **49th consecutive zero-fire on async-discipline rules.** |
| T-P2-009 + T-P2-010 | P2 Phase 9 + 10 (combined) — inspection-tool family (`get_open_editors`, `get_diagnostics`) | empirical 25-40 min / legacy 25-55 min | dual-band | 0:21 (21 min; 19:25:23 → 19:46:06 wall-clock dispatch-to-push) | **sub-band-low** vs empirical (vs low 25: −16%; vs mid 32.5: −35%; vs high 40: −48%) / **sub-band-low** vs legacy (vs low 25: −16%; vs mid 40: −48%; vs high 55: −62%) | **First dispatch post-C-35 scope-lock 2026-05-28.** Combined two-phase dispatch covering both inspection tools in a single round-trip per the dispatch's "inline first, then refactor" guidance. Pre-flight C-13 grep across 7 paths surfaced no contradictions; #6 (gate-invocation site) confirmed clean: gate is invoked ONLY from `delegate.ts:146`, so the new tools naturally bypass without any routing redesign. **Architectural shape:** the daemon→extension push channel was already established by T-P2-008's `IpcServerMessageSchema`; widening it to 3rd and 4th variants (`get_open_editors_request`, `get_diagnostics_request`) was symmetric with the approval pattern. Response envelopes added to `IpcRequestSchema` (3 new variants: 2 responses + 1 generic `extension_tool_error`). The `ExtensionToolRouter` abstraction was built directly (not inline-first-then-refactor) because both tools' send-shape was clearly identical from the spec; the kind-indexed `ExtensionToolResponseByKind<K>` lookup made the routing layer concrete in one go. **Scope delivered:** 4 new daemon source files (extension-router + 2 tools + main.ts wiring) + 4 new daemon test files (router + 2 tools + IPC-server hooks via existing tests) + 1 new extension source (inspection-tools.ts) + 2 new extension test files (inspection handlers + IpcClient routing) + 1 new shared test file (IPC schema regression) + 6 modified source files (shared/{ipc,tools}, daemon/{ipc/server,main}, extension/{ipc/client,extension}). **Two reactive cycles:** (1) initial daemon build failed on generic-inference miss — `send<R extends ExtensionToolResponse>` couldn't narrow R from the literal `expectedKind` arg; switched to kind-indexed `send<K extends ExtensionToolResponse["kind"]>` returning `ExtensionToolResponseByKind<K>` (~2 min); (2) lint flagged 5 unnecessary type assertions in tests + 2 in source (`vscode.window as unknown as VsCodeTabsApi` resolved by direct assignment since VsCodeTabsApi is structurally a subset; `(captured as { request_id: string })` after a null-narrow was redundant) + 3 unsafe-this references in a `this`-bound fake sender (rewrote as closure-captured `fake` variable) — all resolved at-site in one pass (~3 min). **C-35 timing block (mandatory per scope-lock):** 21 minutes wall-clock (20:43 precise), dispatch receipt 19:25:23 → commit-and-push completion 19:46:06. **Below empirical band low edge by 16%.** Drivers: (1) C-13 grep was clean across all 7 paths — no contradictions, no routing redesign trigger; (2) the `ExtensionToolRouter` abstraction was direct rather than inline-then-extract because the shape was clear from the spec — saved the post-refactor cycle; (3) zero §22.5; (4) the kind-indexed generic + the reactive lint sweep consumed only ~5 min total. The pre-existing approval-flow pattern (T-P2-008's daemon→extension push + extension→daemon response) provided a direct template — this is the strongest C-14 signal yet for "task with strong existing template + clean pre-flight grep → deeply sub-band landing." **C-25.b grep → 0 matches** on the repackaged `.vsix` (95.41 KB / 5 files; was 94.02 KB at T-P2-008.8). C-25.c operator runtime smoke (sub-rule 25.3 — no-modal verification on both tool calls + response shape match) pending. **48th consecutive zero-fire on async-discipline rules.** Test totals: 685 passing + 16 skipped (was 614+15; **+71 net**: shared +20, daemon +27, extension +24). Lint clean across 4 workspaces (C-25 fresh-verified). Workspace-root build clean. |
| T-P2-006-followup | Housekeeping — promote settable-single-subscriber-callback pattern to docs | empirical 15-25 min / legacy 15-35 min | dual-band | ≈0:20 (20 min) | **in empirical band (mid)** (vs low 15: +33%; vs mid 20: 0%; vs high 25: −20%) / **in legacy band (mid-low)** (vs low 15: +33%; vs mid 25: −20%; vs high 35: −43%) | Bounded docs + test-verification task with the pattern doc template specified in the dispatch + the 5 canonical sites pre-identified. **C-13 grep:** all 5 settable-field declarations matched expected `public onX?: <signature>;` shape; both method-form listeners on registration.ts also matched expected shape. No shape drift. Per-site test-coverage map: 3 sites (WorkspaceRegistration.onStateChange ×3 — T-P2-006.5; IpcClient.onReconnectAttempt — no public accessor; IpcClient.onApprovalRequest — structurally no C-26 concern) already had sufficient coverage. 2 sites added 2 minimal accessor-read regression tests (IpcClient.onStateChange reads getConnectionState() inside callback; WorkspaceRegistration.onRetryCountChange reads getRetryCount() inside callback). **Scope delivered:** 1 new pattern doc (~250 lines, matching the existing repo template from cross-platform-test-inputs.md), 2 modified test files (+2 tests total), 3 doc edits (C-26 citation in v0.6 candidates table, pending-promotion entry moved from pending to recently-completed in project-state.md, milestones row added). **Calibration:** mid of both bands. Drivers: (1) pattern doc was the bulk of time (~12 min for ~250 lines); (2) C-13 grep + coverage analysis ~5 min; (3) 2 minimal tests ~3 min. No reactive cycles. C-25.b N/A — docs + tests only; no .vsix change. Test totals: 614 passing + 15 skipped (was 612+15; +2 net). Lint clean. **47th consecutive zero-fire on async-discipline rules.** |
| T-P2-008.8 | T-P2-008 follow-up #4 — C-29 registration intent persistence + visible-progress UX | empirical 45-75 min / legacy 45-90 min | dual-band | ≈0:55 (55 min) | **in empirical band (mid)** (vs low 45: +22%; vs mid 60: −8%; vs high 75: −27%) / **in legacy band (mid)** (vs low 45: +22%; vs mid 67.5: −19%; vs high 90: −39%) | Medium-shape task with all 16 ACs pre-resolved and the architectural model (event-driven re-attempt + retry counter UX + composed-callback routing) specified in the dispatch's Fix Specification. Pre-flight C-13 grep (5 paths + daemon idempotency check) took ~10 min and surfaced a finding worth recording: there is no explicit "3-attempt retry counter" in registration.ts — the "give up at attempt 3" pattern in the operator trace was actually the IpcClient's `waitForConnected` 5s polling timeout coinciding with 3 IpcClient reconnect attempts at 1s/2s/4s backoff. Same root effect (registration gives up; IpcClient soldiers on), different mechanism than literally implied. Daemon idempotency confirmed (`ipc/server.ts.removeActiveRegistrationsForSocket` clears stale entries on close) — no daemon-side task required. **Core fix scope:** rewrote `register()` from blocking-await to event-driven (~80 lines net change in registration.ts); composed callback routing in extension.ts; 5th param + first-priority branch in `composeStatusBarText`; tooltip explanation; new `getRetryCount` source + `onRetryCountChange` callback. **UX:** retry-N indicator + sync~spin idiom matching existing convention. **Tests:** 6 new regression cases in a new "intent persistence" describe block (state stays registering across N failures; register fires on eventual connect; retry counter increments + fires onRetryCountChange; counter not updated when already registered; onConnectionStateChanged no-op when already registered; transient failure stays in registering). 4 new status-bar text retry-display cases + 2 new tooltip cases. **Two minor at-site updates:** bulk-script update of 12 direct `composeStatusBarText(...)` calls to add a 5th arg `, 0` (node one-liner); added `getCurrentMode`/`getRetryCount` defaults to status-bar test makeSources factory (was missing currentMode pre-task, surfacing as side effect). **C-25.b grep returned 0 matches** on the repackaged .vsix bundled output. **C-31 added** as v0.6 candidate per dispatch AC #16. **Calibration:** mid of both bands. Drivers: (1) architectural model was specified — no design churn; (2) daemon idempotency check was the dispatch's gating step and resolved cleanly; (3) UX work was a single-branch addition matching existing idiom (sync~spin); (4) test surface was the bulk of time spent (~20 min for 12 cases including the regression scenarios + bulk updates). Continues the C-14 pattern: medium-shape pre-resolved with thorough grep → midpoint landing. **46th consecutive zero-fire on async-discipline rules.** |
| T-P2-008.7 | T-P2-008 follow-up #3 — C-30 session_bypass fix (fork dispatch) | empirical 30-90 min (fork: escalate 15-30 / fix-in-dispatch 60-90) / legacy 30-120 min | dual-band | ≈0:40 (40 min) | **below fix-in-dispatch sub-band low edge** (vs fix-low 60: −33%; vs fix-mid 75: −47%) / **in overall empirical band (mid-low)** (vs low 30: +33%; vs mid 60: −33%) / **below legacy band low edge** (vs low 30: +33%; vs mid 75: −47%) | Fork-in-the-road investigation dispatch. **Decision: FIX-IN-DISPATCH** — data-flow analysis pinned the break unambiguously to `gate.ts:awaitApprovalForDelegation`'s `mode === "session_bypass"` guard (persistent mode defaults `per_call`, so the guard never let the bypass take effect). Tier 1 grep (6 paths) + data-flow walkthrough took ~12 min; the bug was visible on the first read of gate.ts so no runtime tracing needed. **Fix scope:** 4 source files (gate.ts guard + composite-key; server.ts session-header capture; dispatch.ts ToolContext field; delegate.ts threading) + 2 test files (gate.test.ts regression + signature updates; delegate.test.ts stub signature updates). **One reactive defect during verification:** the `bypassKey` separator was a stray NUL byte (from the initial helper write) while `cancelByWorkspace`'s suffix used a space — mismatch surfaced as a failing cancelByWorkspace test (`isSessionBypassed` still true after clear). Diagnosed via `cat -A` byte inspection; normalized both to an explicit `::` separator. ~5 min debug cycle. **C-25.b N/A** (daemon-only change; no extension/.vsix touched — explicitly verified via `git status --short` showing only daemon files). **C-25.c operator smoke (AC-24 R3a/R3b) deferred** to operator post-commit per dispatch. Test totals: 602 passing + 15 skipped (was 598+15 at T-P2-008.5; daemon 360+13 was 356+13; +4 net from C-30 regression cases). Lint clean across 4 workspaces (C-25 fresh-verified). **Calibration:** landed at 40 min — below the fork's fix-in-dispatch sub-band (60-90) because the root cause was unambiguous on first read (no multi-hypothesis tracing) and the fix was a localized guard-drop + mechanical session-id threading. The NUL-separator debug cycle was the only unplanned cost. The dispatch's escalate-path estimate (15-30) would have applied had the bug needed runtime tracing; since it didn't, the actual landed between the two sub-bands. **45th consecutive zero-fire on async-discipline rules.** |
| T-P2-008.6 | T-P2-008 follow-up #2 — CLAUDE_BRIDGE_DEBUG diagnostic instrumentation for C-29 | empirical 15-30 min / legacy 15-45 min | dual-band | ≈0:18 (18 min) | **in empirical band (mid-low)** (vs low 15: +20%; vs mid 22.5: −20%; vs high 30: −40%) / **in legacy band (low-mid)** (vs low 15: +20%; vs mid 30: −40%; vs high 45: −60%) | Bounded additive task — diagnostic logging only, no behavior changes. All 8 instrumentation sites + helper shape + test matrix pre-specified in dispatch. Pre-flight C-13 grep (#1-#6) confirmed clean codebase: no existing CLAUDE_BRIDGE_DEBUG, no `[cb-diag]` / `[claude-bridge]` console prefix, no diag.ts/debug.ts. Both predicted ratholes (env-var resolution timing in CJS bundle; locating implicit state-transition sites) didn't fire — `process.env.CLAUDE_BRIDGE_DEBUG` resolves cleanly at module-load in the CJS bundle (no special handling needed), and registration.ts's state machine has an explicit central `setState` mutator so one centralized `diag()` call covers all 13 transition sites cleanly. **Scope delivered:** 1 new source file (`src/diag.ts` ~14 lines), 1 new test file (`tests/diag.test.ts` ~75 lines, 6 cases vs AC #6 minimum 2 — expanded for env-var-value matrix), 3 modified source files (`extension.ts` +2 diag calls, `registration.ts` +3 diag calls inside setState + 2 catch boundaries, `ipc/client.ts` +6 diag calls). 2 doc edits (`project-state.md` C-29 row + C-25.c sub-rule, `milestones.md` T-P2-008.6 entry). **C-25 fresh-verification protocol applied:** `npm run lint` ran clean immediately. **C-25.b verification:** `unzip -p ... \| grep -c "@claude-bridge/"` → 0 matches (exit 1). **New C-25.c sub-rule** documented per dispatch AC #13: operator-performed runtime smoke for build/packaging-pipeline tasks closes the runtime-evidence gap that C-25.b can't cover. Test totals: 114 passing + 15 skipped extension-side (+6 net); workspace-root 604 passing + 15 skipped (+6). Lint clean across 4 workspaces. **C-14 datapoint:** bounded additive task with all decisions pre-resolved + clean pre-flight grep + zero §22.5 → landed at empirical mid-low. Continues the pattern: dispatch-fully-specified tasks consistently land in the lower half of the empirical band, with the legacy band's lower half also tight when scope is genuinely small. **44th consecutive zero-fire on async-discipline rules.** |
| T-P2-008.5 | T-P2-008 follow-up — Extension `.vsix` bundling fix via esbuild (closes C-28) | empirical 30-60 min / legacy Small-medium 30-90 min | dual-band | 0:25 (25 min) | **below empirical band low edge** (vs low 30: −17%; vs mid 45: −44%; vs high 60: −58%) / **below legacy band low edge** (vs low 30: −17%; vs mid 60: −58%; vs high 90: −72%) | Bounded-fix task with the fix shape fully specified in the dispatch (esbuild config + tsconfig noEmit + script update + regression test). Pre-flight C-13 grep confirmed all assumptions (no existing bundler, no esbuild dep in monorepo, .vscodeignore + package.json shape exactly as expected). Scope: 1 new source file (`esbuild.config.mjs` ~25 lines), 1 new test file (`build-output.test.ts` ~50 lines with 4 cases), 4 modified files (package.json, tsconfig.json, .vscodeignore, docs). **One small at-site finding:** dropped `composite: true` from tsconfig (would conflict with `noEmit: true`); confirmed safe via grep — no other tsconfig referenced this composite project. **Decision: format: cjs** chosen over format: esm despite the dispatch's parenthetical "match current main" because the spec line was unambiguous and CJS is universally supported across VS Code versions; required dropping `"type": "module"` from extension package.json so Node doesn't refuse the `.js` extension as CJS. **Verification evidence captured per AC #8 and new C-25.b sub-rule:** `unzip -p` + `grep -c` → 0 matches on the unpacked bundle. **C-25.b sub-rule added** to v0.6 candidates: bundled-artifact verification requires grep-evidence at verdict time. Test totals: 598 passing + 15 skipped (was 594+15; +4 net all from build-output.test.ts). Lint clean across 4 workspaces. **Calibration: below empirical low edge.** Drivers: (1) all decisions pre-resolved with concrete config snippet in dispatch; (2) zero §22.5; (3) reactive at-site composite/noEmit conflict resolved with one grep-and-drop (no debug cycles); (4) bundled-artifact verification was straightforward — esbuild's bundle: true with external: ["vscode"] is the canonical pattern. Continues the C-14 pattern: bounded-fix + decisions pre-resolved + clean pre-flight grep → sub-band landing. **43rd consecutive zero-fire on async-discipline rules.** |
| T-P2-008 | Phase 8 — per-delegation approval flow (auto / per_call / session_bypass) | empirical 70-95 min / legacy Hard 120-240 min | dual-band | 1:38 (98 min) | **at empirical band high edge** (vs low 70: +40%; vs mid 82.5: +19%; vs high 95: +3%) / **below legacy band low edge** (vs low 120: −18%; vs mid 180: −46%; vs high 240: −59%) | Largest P2 task to date by surface (8 source files modified, 2 new daemon source files, 1 new extension source file, 5 new test files, 2 test extensions). Pure-code-with-bounded-discovery shape. All 14 sub-decisions pre-resolved; zero §22.5 fired. Multi-grep pre-verification removed two would-have-been misframings (approval infrastructure existence; daemon-initiated message handling). Scope delivered: novel `IpcServerMessageSchema` discriminated union for daemon→extension push (first such mechanism in the codebase); `PendingApprovalRegistry` 5-min-timeout state machine + `ApprovalGateImpl` composition + delegate-handler gate insertion + IPC server-side handlers + sendServerMessage method + main.ts shutdown-layer integration (logs `approval layer stopped`); extension-side IpcClient parse-discrimination + onApprovalRequest callback (4th instance of single-subscriber callback pattern) + modal UX + status-bar-menu mode-change item + secondary QuickPick + applyMode IPC round-trip. **Two reactive cycles:** (1) initial build failed on missing `mode` field in `IpcResponseShape` interface in registration.ts — added the optional field, simplified the redundant `as` casts at-site (~3 min); (2) initial test run had 1 unhandled rejection in gate's requestApproval error path — pending promise cancellation rejected without an awaiter — added a pre-attached `promise.catch(() => undefined)` swallow handler before the explicit re-throw (~4 min). 3 lint fires in new test/source files resolved at-site (~3 min): redundant `as` cast in status-bar-menu dispatch, unsafe-call on `args.slice(2)` rewritten as `toHaveBeenCalledWith`, non-null `!` replaced with explicit undefined-guard. **C-26 compliance:** new `currentMode` field assignment placed BEFORE `setState("registered")` at both register_workspace_ok call sites (mirrors T-P2-006.5 invariant). **C-25 compliance:** fresh `npm run lint` + `npm run test` runs (not memory-asserted streak). Test totals: 594 passing + 15 skipped (was 532+15; +62 net — daemon +38: 19 gate + 10 pending + 8 delegate approval + 3 integration approval-flow; extension +24: 13 approval-modal + 11 from status-bar-menu mode-change cases including sources interface widening). .vsix repackaged at 20.91 KB / 12 files (was 18.03 / 11; added approval-modal.js). Daemon.log shows new `approval gate initialized` info line. **Calibration:** the empirical-band-high-edge landing matches the dispatch's prediction reasoning ("midpoint per refined C-14 for large pre-resolved-with-thorough-grep tasks"); the two reactive cycles consumed ~10 min of band headroom, landing close to the band's upper edge rather than midpoint. The legacy-band miss (−18% below low edge) is consistent with the P2-era systematic over-prediction for legacy Hard work — empirical bands continue to be the calibrated reference. **42nd consecutive zero-fire on async-discipline rules.** |
| T-P2-006.5 | T-P2-006 follow-up — field-vs-state-ordering fix in WorkspaceRegistration | empirical 10-15 min / legacy Trivial+setup 10-20 min | dual-band | 0:18 | **above empirical band high edge** (vs low 10: +80%; vs mid 12.5: +44%; vs high 15: +20%) / **in legacy band (high-edge)** (vs low 10: +80%; vs mid 15: +20%; vs high 20: −10%) | Bounded-fix task with all 3 sub-decisions pre-resolved. Pre-flight grep + read confirmed exact defect sites: 3 defective setState calls (lines 117/118, 147/148, 175/176) where field assignment followed setState. 10 other setState sites audited; all transitions to states whose render doesn't depend on a class field (`unregistered`, `registering`, `needs_trust`, `trust_denied`). Scope: 3 surgical swaps with invariant comments (~12 lines net in registration.ts); 3 regression tests in registration.test.ts (~60 lines); .vsix repackage + reinstall. **Pre-fix verification:** temporarily reverted the line 117/118 swap, ran extension test suite, got expected failure `expected null to be "myproject-54ab07"`; restored fix. **vsce-tooling quirk surfaced:** default `vsce package` errored with `invalid relative path: extension/../../tsconfig.base.json` (composite tsconfig extends chain rejected by vsce traversal); workaround `npx vsce package --no-dependencies` succeeded — runbook-level note, not a v0.6 candidate. **C-25 fresh-verification protocol applied:** `npm run lint --workspaces --if-present` ran clean across all 4 workspaces; verbatim output included in Form B report. **Closes C-26**. No §22.5 fired. **Calibration: above-band landing (+20% vs high edge).** Drivers: (1) C-25 compliance overhead — fresh build + lint sweep across all 4 workspaces is ~2-3 min of additional verification time per task vs the memory-asserted shortcut; (2) the pre-fix-verification step (revert + run test + see failure + restore + re-run) was ~3 min; (3) the vsce-tooling quirk consumed ~2 min of debug + retry. Net real coding work was ~8-10 min — in-band. The above-band landing is the empirical cost of C-25 protocol + pre-fix-verification protocol; v0.6 should either fold these into the empirical band for bounded-fix tasks or define a "rigorous-verification subband" that's 5-7 min wider than the baseline. **C-14 datapoint:** when bounded-fix scope is genuinely bounded AND all verification protocols fire (lint sweep, pre-fix-test-verify, .vsix repackage, manual install), the empirical band needs ~+5 min headroom that current bands don't model. Test totals: 535 passing + 15 skipped (was 532+15; +3 net all from new regression tests). |
| T-P2-007.5 | T-P2-007 follow-up — Windows path case-insensitivity at workspace registry lookup | empirical 20-30 min / legacy Small 30-60 min | dual-band | 0:33 (≈20 in-scope + ≈13 user-requested out-of-scope lint cleanup) | **In-scope alone: below empirical band low edge** (vs low 20: 0%; vs mid 25: −20%; vs high 30: −33%) / **below legacy band low edge** (vs low 30: −33%; vs mid 45: −56%; vs high 60: −67%). **Combined (incl. lint cleanup): in empirical band (high)** (+10%/+32%/+10% over low/mid/high) | Bounded-fix task with all 5 sub-decisions pre-resolved. Pre-flight grep confirmed dispatch's single-comparison-site claim (`store.ts:67-69`) and absence of existing normalization helpers. Scope: `packages/shared/src/path.ts` new (~17 lines), `packages/shared/src/index.ts` re-export, `packages/shared/tests/path.test.ts` new (6 tests), `packages/daemon/src/workspace/store.ts` (+normalizeAbsPath import + Logger type-import + dedupeOnLoad private method + findByPath two-side normalization + constructor logger param), `packages/daemon/src/main.ts` (1-line: pass logger to WorkspacesStore constructor), `packages/daemon/tests/workspace/store.test.ts` (+7 tests including AC-13 dedupe + log shape + idempotency + 3-way collision). **Closes C-24**. Manual Windows verification PASS via daemon-restart against pre-existing 3-entry workspaces.json: post-startup file has 2 entries (canonical 54ab07 retained, lowercase 06e146 removed); daemon.log shows warn line with all 4 fields. **Mid-task user-requested out-of-scope:** fix 10 pre-existing lint errors in extension + CLI test files. Errors latent at commit b0b8586 (T-P2-007 verdict's "lint clean" claim was contradicted by a fresh `npm run lint`). Fixed at-site without semantic changes: removed unnecessary `as never`, `as unknown as T`, redundant `unknown \| undefined` union, async modifier on no-await test, replaced non-null `!` with const-aliased narrowing. **New v0.6 candidate C-25** (verdict-claim verification: streak counters need fresh tool runs, not memory). **C-14 datapoint:** bounded-fix task with all decisions pre-resolved + clean pre-flight grep → in-scope work landed at empirical-band low edge (T-P2-004.6 precedent). Out-of-scope addition consumed +13 min, pushing combined into mid-band — out-of-scope user requests are an unpredictable variance driver not yet captured in either band. Test totals: 532 passing + 15 skipped (was 519+15; +13 net — daemon +7 from store.test.ts case+dedupe tests, shared +6 from new path.test.ts). Lint clean across 4 workspaces (genuinely-fresh-verified this time, including the extension + CLI files that were silently breaking previously). |
| T-P2-007 | Phase 7 — workspace registry replacement (multi-workspace + strict rejection) | empirical 35-50 min / legacy Medium-fresh 60-120 min | dual-band | 0:40 | **in empirical band (mid-low)** (vs low 35: +14%; vs mid 42.5: −6%; vs high 50: −20%) / **below legacy band low edge** (vs low 60: −33%; vs mid 90: −56%; vs high 120: −67%) | Medium-shape pre-resolved task. Pre-flight grep confirmed all dispatch claims about registry interface + activeRegistry key + delegate.ts shape + construction sites. Scope: shared/delegation.ts (1-line schema change), daemon/workspace/registry.ts (full rewrite ~75 lines), daemon/ipc/server.ts (export ActiveRegistration + new getActiveRegistry accessor), daemon/main.ts (registry construction reorder + ipcServerRef thunk pattern for forward-declared IpcServer dependency), daemon/mcp/tools/delegate.ts (collapse 2-branch error to single 503 path), 4 test surfaces (registry.test.ts full rewrite +12 tests including AC-12 daemon-restart integration; delegate.test.ts switch from StubWorkspaceRegistry to inline makeTestRegistry helper + 3 case updates + 1 new for required-workspace; sdk-runner.test.ts same inline-helper switch; shared/delegation.test.ts 4 cases adjusted for required workspace + 1 new rejection case). **One forward-declaration subtlety:** registry construction needs both WorkspacesStore (available early) and IpcServer.getActiveRegistry (constructed later). Resolved via ipcServerRef.current mutable holder + thunk; lean per dispatch's Option (A); empty-Map default safe because T-P2-007's resolve() doesn't read activeRegistry (forward-looking for P2-008+). **Closes C-23** (daemon-restart-against-pre-populated-store latent defect from T-P2-003); AC-12 integration test added; no §22.5 fired. Test totals: 519 passing + 15 skipped (was 516+15 before T-P2-007; +3 net — daemon +2 from registry rewrite consolidating 10→12 tests, shared +1 from new rejection-of-missing-workspace test). Lint clean across 4 workspaces. 41st consecutive zero-fire on async-discipline rules. C-14 datapoint: medium-shape pre-resolved task with multi-grep preverification landed below midpoint (−6%), confirming pattern that thorough pre-flight grep keeps medium tasks slightly under midpoint. |
| T-P2-006 | Phase 6 — status bar item with workspace identifier + daemon state | empirical 40-55 min / legacy Medium-fresh 60-120 min | dual-band | 0:48 | **in empirical band (mid)** (vs low 40: +20%; vs mid 47.5: +1%; vs high 55: −13%) / **below legacy band low edge** (vs low 60: −20%; vs mid 90: −47%; vs high 120: −60%) | Medium-shape pre-resolved task. All 8 sub-decisions locked pre-conversation. Scope: 2 new source modules (status-bar.ts ~165 lines + status-bar-menu.ts ~190 lines), 2 modified source files (ipc/client.ts + registration.ts: onStateChange field + setState helper + refactor 5 + 13 scattered state writes), package.json contribution, mock extensions (~50 lines), 4 test surfaces (+35 new tests across status-bar, status-bar-menu, ipc-client.onStateChange, registration.onStateChange). **One small at-site fix:** dispatch's Decision 6 table mentioned a `"no_workspace"` registration state that doesn't exist in the actual `RegistrationState` union (only appears as `RegistrationResult.state`; internal state never transitions to it). Removed dead branches from `composeStatusBarText` + `trustLabel`; added comment that `makeStatusBar.refresh()` hides the item upstream for no-workspace case. Pattern-promotion follow-up logged: third confirmed use of "settable single-subscriber callback field" pattern (onReconnectAttempt + 2 onStateChange). **C-14 datapoint:** medium pre-resolved task with one small at-site fix landed at empirical-band midpoint (+1%). Pattern firms further: medium-shape pre-resolved → midpoint; small-shape pre-resolved → low edge. Test totals: 516 passing + 15 skipped (was 481+15 before T-P2-006; +35 new). Lint clean across 4 workspaces. 40th consecutive zero-fire on async-discipline rules. .vsix repackaged at 17.88 KB / 11 files (was 13.51 KB / 9 files; added 2 new compiled modules). No new v0.6 candidates surfaced. |
| T-P2-005 | Phase 5 — daemon-not-running detection UX + actionable retry notification | empirical 25-40 min / legacy Medium-fresh 60-120 min | dual-band | 0:30 | **in empirical band (mid)** (vs low 25: +20%; vs mid 32.5: −8%; vs high 40: −25%) / **below legacy band low edge** (vs low 60: −50%; vs mid 90: −67%; vs high 120: −75%) | Pure-code-with-bounded-discovery task. All 8 sub-decisions pre-resolved. Scope: 3 source files (ipc/client.ts callback field + invoke; daemon-lifecycle.ts +2 exports; extension.ts rewire) + 13 new tests across 2 test files. **One small at-site refactor:** extracted the dispatch's inline handler lambda into a `makeDaemonNotRunningHandler(context, deps?)` factory in `daemon-lifecycle.ts` so the once-per-session guard becomes closure-local (testable in isolation) rather than module-scope (requires full-activate testing). The factory pattern is within §22.5-NOT-fire scope per dispatch ("test organization — group as makes sense"). **One async-flush adjustment in tests:** `await Promise.resolve()` x 2 wasn't enough to flush the IIFE's `await showWarning` → `await runStart` chain; switched to `await new Promise(r => setImmediate(r))` per the T-0012 daemon-test microtask-flush precedent. 1 test surface fix. **C-14 datapoint:** small-to-medium pre-resolved task with one at-site refactor landed at empirical-band midpoint (slightly below: −8%). Pattern continues to firm: pre-resolved tasks with a small refactor opportunity sit at midpoint; pre-resolved tasks that are pure scaffolding sit at low edge. Test totals: 481 passing + 15 skipped (was 468+15 before T-P2-005; +13 new). Lint clean across 4 workspaces. 39th consecutive zero-fire on async-discipline rules. .vsix repackaged at 13.51 KB (was 12.27 KB; same 9 files). No new v0.6 candidates surfaced. |
| T-P2-004.6 | `shell: true` on Windows for CVE-2024-27980 workaround | empirical 10-15 min / legacy Trivial+setup 10-20 min | dual-band | 0:08 | **below empirical band low edge** (vs low 10: −20%; vs mid 12.5: −36%; vs high 15: −47%) / **below legacy band low edge** (vs low 10: −20%; vs mid 15: −47%; vs high 20: −60%) | Second follow-up on the T-P2-004 daemon-lifecycle defect. Bounded scope: 2 source edits + 1 test assertion update + 4 doc edits. Zero §22.5 fired. Pure mechanical pattern application — defect well-characterized in dispatch (CVE-2024-27980 cited; F1 vs F2 narrowing audit complete). **C-14 datapoint:** bounded-fix task with all decisions pre-resolved → 4-min code work, ~2 min doc, ~2 min verification. Pattern firms further: "post-task targeted fix from manual verification with complete pre-analysis" sub-bucket lands deep below both bands' low edges. v0.6 candidate. **Notable orchestrator self-flag:** the dispatch itself names the orchestrator narrowing error (T-P2-004 → 004.5 → 004.6) as v0.6 candidate C-22 — methodology lesson rather than implementation surprise. Test totals: 468 passing + 15 skipped (unchanged count; one assertion shape adjusted from strict `{stdio, shell: false}` to `expect.objectContaining({stdio})`). Lint clean across 4 workspaces. 38th consecutive zero-fire on async-discipline rules. .vsix repackaged at 12.27 KB (was 12.01 KB; same 9 files). |
| T-P2-004.5 | Windows `.cmd` shim resolution for `claude-bridge` CLI | empirical 15-25 min / legacy Small 30-60 min | dual-band | 0:14 | **below empirical band low edge** (vs low 15: −7%; vs mid 20: −30%; vs high 25: −44%) / **below legacy band low edge** (vs low 30: −53%; vs mid 45: −69%; vs high 60: −77%) | Post-T-P2-004 follow-up. Real defect surfaced by manual UI verification (Windows host: bare `spawnSync("claude-bridge")` didn't auto-resolve PATHEXT in the VS Code extension host). Fix is bounded: F2 explicit-candidate iteration + field rename + 3 new tests. **One pre-existing lint nit** in T-P2-004's mock (recommendedTypeChecked flagged a load-bearing generic cast as unnecessary; inline-disabled per sdk-runner.ts precedent — ~1 min). **No reactive scope deviations.** **C-14 datapoint:** small pre-resolved task with zero §22.5 fired landed below empirical low edge by 7% — even tighter than T-P2-004's "low-edge" landing. Pattern confirmed: when a follow-up has truly bounded scope + no discovery surface + no §22.5, the empirical band's low edge becomes a soft ceiling, not a floor. v0.6 sub-bucket for "post-task targeted fix from manual verification" candidate. Test totals: 468 passing + 15 skipped (was 465+15; +3 net). Lint clean across 4 workspaces. 37th consecutive zero-fire on async-discipline rules. .vsix repackaged at 12.01 KB (was 11.68 KB; same 9 files). Two v0.6 candidates logged (C-20 Windows-shim pattern, C-21 Form-of-Test-Bug from mock-vs-production drift). |
| T-P2-004 | Phase 4 — daemon-lifecycle commands + SecretStorage API key | empirical 30-45 min / legacy Medium-fresh 60-120 min | dual-band | 0:30 | **at empirical band low edge** (vs low 30: 0%; vs mid 37.5: −20%; vs high 45: −33%) / **below legacy band low edge** (vs low 60: −50%; vs mid 90: −67%; vs high 120: −75%) | Smaller surface than T-P2-003 (only extension-side; no new IPC variants, no new persistence layer). Pure-code-with-bounded-discovery shape. Scope: 1 new module (`daemon-lifecycle.ts` ~190 lines), extension.ts wiring (+ autoStart hook), package.json contributions, 1 new test file (15 tests), mock extensions (`showInputBox` + `secrets` + `getConfiguration`). **One reactive fix mid-task:** VS Code's `SecretStorage.get` returns `Thenable<T>` not `Promise<T>` — caught by build TS2322; resolved by switching `SecretsApi` interface to `PromiseLike<T>`. ~5 min of debug + fix. Codified as v0.6 candidate C-19. **Option A spawn approach** kept surface narrow as predicted: all daemon-spawn logic stays in CLI's existing `startCommand`; extension is thin. **C-14 datapoint:** small-to-medium pre-resolved task landed at empirical band low edge (predicted by dispatch). The pattern continues to firm: small + pre-resolved → lower-half; genuinely-medium + pre-resolved → midpoint (T-P2-003); small post-design task with one API surprise → low edge with debug headroom consumed. **Test totals:** 465 passing + 15 skipped (was 450+15; +15 new T-P2-004 tests). 36th consecutive zero-fire on async-discipline rules. **.vsix at 11.68 KB / 9 files** (was 8.34/8). Lint clean across all 4 workspaces. Legacy band miss yet again — 50% below low edge; dispatch's "Medium-fresh 60-120" was the right legacy bracket but empirical bands continue to dominate as the planning reference. v0.6 should adopt empirical-bands-as-primary alongside legacy-as-annotation. |
| T-P2-003 | Phase 3 — workspace registration + trust prompt + workspaces.json store | empirical 40-60 min / legacy Medium-fresh 60-120 min | dual-band | 0:50 | **in empirical band (midpoint)** (vs low 40: +25%; vs mid 50: 0%; vs high 60: −17%) / **in legacy band (low edge)** (vs low 60: −17%; vs mid 90: −44%; vs high 120: −58%) | Largest P2 task so far by file count and module surface. Pure-code-with-bounded-discovery shape. Scope: 7 new files (identifier.ts, store.ts, registration.ts, trust-prompt.ts + 3 test files); 9 modified files (shared/ipc.ts + workspace.ts; daemon main.ts + ipc/server.ts + config/paths.ts; extension extension.ts + ipc/client.ts; mock + daemon ipc test). Three new IPC request kinds + three new response kinds; daemon active-registry Map + 3 new handlers + socket-close cleanup; extension IpcClient extended with single-flight `request<R>()` + post-hello data multiplexing (the dispatch's §22.5 trigger — extending vs refactoring; chose extend at-site as the natural composition step, ~30 lines of new code, well within the implicit threshold). One mid-task test fix (destructuring indexed wrong response). **First task where empirical and legacy bands overlap meaningfully** (40-60 vs 60-120 — the empirical band's upper edge equals the legacy band's lower edge). Landed at empirical midpoint = legacy low edge: **the bands converge for genuinely-medium pre-resolved tasks**. C-14 candidate gets a 3rd datapoint here: pre-resolved task lands at empirical midpoint (not lower-half), strengthening the v0.6 sub-bucket evidence rather than the "always lower-half" reading. **Test totals: 450/450 passing + 15 skipped** (was 421+15 before T-P2-003; +29 new across 4 test surfaces: 11 identifier + 7 store + 5 daemon IPC handlers + 6 extension registration). Lint clean across all 4 workspaces. 35th consecutive zero-fire on async-discipline rules. **.vsix at 8.34 KB**, 8 files (was 4.96 KB / 6 files after T-P2-002); reinstalled on Windows. AC-15 verified: P1 stub `registry.ts` byte-identical (`git diff` empty). **§22.5 IpcClient extension call:** dispatch flagged this as a possible trigger; in-context decision was clear (extend natural composition, not refactor architecture); resolved at-site without consultation cycle. No reactive scope deviations. **Calibration: dispatch's prediction reasoning held precisely** — "first time we hit Medium-fresh as the right legacy band; calibration log entry will be data on whether C-14 (lower-half-landing on pre-resolved tasks) holds for genuinely-medium tasks too." Answer: lower-half pattern is real for small tasks but genuinely-medium pre-resolved tasks land at midpoint, not lower-half. v0.6 should refine C-14 with this finding. |
| T-P2-002 | Phase 2 — IPC hello/versioning protocol with lockstep CLI | empirical 25-35 min / legacy Medium-fresh 60-120 min | dual-band | 0:32 | **in empirical band (mid-high)** (vs low 25: +28%; vs mid 30: +7%; vs high 35: −9%) / **below legacy band low edge** (vs low 60: −47%; vs mid 90: −64%; vs high 120: −73%) | First P2 implementation task with real cross-package surface (shared schemas + daemon gate + CLI prelude + extension client + 4 test suites). Pure-code-with-bounded-discovery shape. Scope: extended shared/ipc.ts with hello + hello_ok + error.reason variants; daemon's `dispatchLine` gated on per-Socket `ConnectionState` Map; CLI's `performIpc` two-phase state machine (awaiting_hello_ok → awaiting_response) + typed `IpcClientVersionMismatchError` with exitCode 4; new extension `IpcClient` with reconnect/backoff + version-mismatch showErrorMessage UX. **One §22.5 consultation** on error-variant shape — user chose optional `reason` field extension; cleanest backwards-compatible path. **Test surface +13:** daemon ipc/server +6 (hello-gate paths + checkVersion unit), CLI ipc-client +1 (version-mismatch via mock server), extension ipc-client +6 (state machine via FakeSocket). Daemon `rpc()` and `rpcDouble()` test helpers refactored to include hello prelude transparently; `rpcRaw()` added for non-hello-first hello-gate tests. **3 lint fires in extension tests** caught by type-aware rules (unused `_enc` param; unsafe `any` cast in `ReturnType<typeof require>`; unnecessary type assertion). All resolved in 1 cycle. 34th consecutive zero-fire on async-discipline rules for production code. **.vsix repackaged:** 6 files, 4.96 KB (was 5 files, 2.29 KB — added dist/ipc/client.js at 7.12 KB). Reinstalled on Windows. **Calibration:** +7% above empirical-mid (30). Dispatch's prediction reasoning held: "could land at 25 if discovery drops out cleanly, or 35 if mock-shape or vitest+vscode-alias has wrinkles." The §22.5 consultation cycle (~3 min) + the daemon-test-helper refactor (~3 min to add hello prelude transparently) + the 3 lint-cleanup cycle (~2 min) consumed the upper-band headroom. Net: real code work ~24 min, overhead ~8 min, total 32. Legacy band −64% below midpoint — yet another P2-era datapoint confirming the systematic over-prediction. C-14 candidate strengthens (2 datapoints; needs 2-3 more). |
| T-P2-001 | Phase 1 — extension scaffolding + vitest test harness | empirical 25-40 min / legacy Medium-fresh 60-120 min | dual-band | 0:25 | **at empirical band low edge** (vs low 25: 0%; vs mid 32.5: −23%; vs high 40: −38%) / **below legacy band low edge** (vs low 60: −58%; vs mid 90: −72%; vs high 120: −79%) | First P2 implementation task. Pure-code-with-discovery shape: scaffolding mechanical, but mock surface + vsce naming + eslint glob surfaced bounded discovery. Scope: 8 new files in `packages/extension/` (package.json, tsconfig.json, vitest.config.ts, .vscodeignore, README.md, src/extension.ts, tests/mocks/vscode.ts, tests/extension.test.ts). One §22.5 consultation (vsce rejected scoped `@claude-bridge/extension`; user chose flat `claude-bridge-extension`). One trivial root eslint glob extension (`packages/*/tests/mocks/*.ts`) for the first non-`.test.ts` file in any tests/ tree. **vitest+vscode-alias worked first try** with `fileURLToPath(new URL(...))` shape — no wrinkles. **Test results:** 2/2 vitest passing in 1.14s. **Build:** tsc -b composite clean. **Install verified Windows:** `code.cmd --install-extension` succeeded; layout confirmed at `%USERPROFILE%/.vscode/extensions/undefined_publisher.claude-bridge-extension-0.1.0/`. WSL verification deferred to T-P2-012 per dispatch. No production-code semantic changes elsewhere; 280 daemon tests still pass. 33rd consecutive zero-fire on async-discipline rules. **Calibration:** landed at empirical low edge — the §22.5 consultation cycle (~3 min) and the lint debug (~2 min) consumed the upper-band headroom, leaving ~20 min of pure scaffolding work in line with dispatch's prediction reasoning ("could land at 25 min if the mock surface drops out cleanly, or 40 if vitest+vscode-alias has wrinkles"). Net: mock surface dropped out cleanly, but vsce naming was the dispatch-anticipated §22.5 trigger that fired. Legacy band -72% below midpoint — yet another P2-era datapoint confirming legacy bands systematically over-predict scope-pre-resolved pure-code work. v0.5 §5.2 empirical bands hold. |
| T-P2-000-review | Pre-P2 review pass (design doc + build plan) | empirical 20-30 min / legacy Medium 60-120 min | dual-band | 0:22 | **in empirical band (mid)** (vs low 20: +10%; vs mid 25: −12%; vs high 30: −27%) / **below legacy band low edge** (vs low 60: −63%; vs mid 90: −76%; vs high 120: −82%) | First post-P1, pre-P2 task. **First review-shape task** in the project — different cost profile from any prior datapoint. Scope: read two new design docs (224 + 464 lines = 688 lines total), cross-check against P1 codebase (`packages/shared/src/config.ts`, `packages/daemon/src/ipc/protocol.ts`), verify cross-references, evaluate empirical predictions against P1 calibration data, surface findings as two review appendices. **2 low-risk corrections applied** (config.workspaces→workspace; Q4 status code consistency); **16 substantive concerns surfaced** for orchestrator follow-up (7 design + 9 build plan); confidence assessment "80-85% ready; recommend short clarification round before T-P2-001 dispatches." No code changes; build/lint untouched; 280 daemon tests preserved; 33rd consecutive zero-fire on async-discipline rules. **Calibration insight:** review work is heavier than doc-extension (T-P1-013 at 20 min for content production from synthesized knowledge) because it requires close reading + cross-verification + judgment calls about what to fix vs surface. The "+10% above empirical-low" landing reflects this: the task is bounded but each finding-discovery cycle has real cost (read → verify against code → decide low-risk-correct vs surface-as-concern → write up). **v0.6 candidate signal:** the report shape for review tasks is dispatch-defined (Form B), but the heavy audit-trail of corrections + concerns + appendix text suggests a Form C variant might help. Noted in project-state recently-completed entry as the first v0.6 candidate from P2-era work. **Legacy band miss is the largest yet observed in P1+** (−76% vs midpoint). Legacy bucket Medium 60-120 is wildly off for read-and-critique work; a doc-review-shape sub-bucket would be a v0.6 addition. |
| T-P1-015 | Post-gate README refresh | empirical 10-15 min / legacy Trivial+ 15-30 min | dual-band | 0:09 | **below empirical band low edge** (vs low 10: −10%; vs mid 12.5: −28%; vs high 15: −40%) / **below legacy band low edge** (vs low 15: −40%; vs mid 22.5: −60%; vs high 30: −70%) | Twelfth dual-band datapoint — **first post-gate task**. Pure-doc extension; smaller than T-P1-013 (one file, condensation rather than production). Scope: 5 targeted Edit calls to `README.md` updating project description, status line, "What is this?" + gate table, prerequisites (Node 22 LTS + ANTHROPIC_API_KEY), MCP-client section (four tools instead of one), and dive-deeper pointer list. README went 110 → 116 lines (+6 net; content was replaced and expanded in place). No new sections added per dispatch Decision 2 (README stays an elevator pitch, not a duplicate of runbook). All internal links verified to resolve against existing files. No code changes; build/lint untouched; 32nd consecutive zero-fire on async-discipline rules preserved. **Calibration:** the dispatch's prediction reasoning ("post-gate cleanup tasks should converge on the empirical floor") was correct in direction but conservative — the actual landed *below* the empirical low edge by 10%. Reason: scope was even more bounded than estimated (5 small edits, all content already mentally cached from T-P1-013/T-P1-014 doc work). Confirms the v0.5 §10 candidate "doc-extension sub-bucket distinct from design-from-scratch": post-gate housekeeping where all source material is freshly cached runs at typing speed × edit count, not at content-generation speed. Both bands' floors should drop further for "post-gate-doc-touch-up" task shape. |
| T-P1-014 | Phase 14 — P1 gate close + design-doc-debt sweep | empirical 15-25 min / legacy Medium-mature 30-90 min | dual-band | 0:22 | **in empirical band (mid-high)** (vs low 15: +47%; vs mid 20: +10%; vs high 25: −12%) / **below legacy band low edge** (vs low 30: −27%; vs mid 60: −63%; vs high 90: −76%) | Eleventh dual-band datapoint — **final P1 task**. Pure-doc + minor-code-comment task. Scope: apply 11 doc-debt items across `docs/design/02-p1-delegation.md` (items 1/3/5/6/7/8/10 — AC-2 FIFO-index rewrite, 32KB-cap phrase removal, DiffResult.diff opaque-text contract, ExitPlanMode/READ_ONLY_DISALLOWED_TOOLS belt-and-suspenders paragraph, wait_ms cap divergence clarification, snapshot caps + binary detection threshold, package rename); also items 6/11 across `p1-build-plan.md` + `00-overview.md` + git-tracked v0.5 doc; item 4 updated 5 `P1-only` markers in 4 files; item 2 created `docs/snapshot/retroactive-notes.md` (T-P1-003 DailyTimer coverage gap) since the P1-open snapshot lacked the verdict text; item 9 absorbed into the P1-close snapshot's Performance datapoint section. Produced `docs/snapshot/orchestrator-context-p1-close.md` (~280 lines, 11 required sections per P0-close template adapted to P1 content). **No reactive deviations.** Build clean; lint clean (no production semantics changed — only comment updates); 280 daemon tests preserved; 31st consecutive zero-fire on async-discipline rules. **Calibration:** the +10% over empirical-mid is real — sweep-then-snapshot pattern with 11 independent items has more context-switching overhead than monolithic doc production. Each item required locating the right section + understanding the original intent + writing the replacement; the snapshot then required cross-referencing all 14 P1 task verdicts + AC mapping + calibration table aggregation. Legacy band's "Medium-mature 30-90" anchor is way off for this kind of mechanical-reconciliation work; the 76% below legacy-high signal is consistent with T-P1-013 (doc-extension also far below legacy). **v0.5 §10 candidate:** reinforces the "doc tasks compress legacy bands" finding from T-P1-013. **Final P1 streak:** 11 dual-band datapoints; 4 in-band, 7 sub-band; empirical bands reliably tighter than legacy. v0.5 §5.2 empirical-band table is the calibrated reference going forward into P2. |
| T-P1-013 | Phase 13 — operator runbook + P1 walkthrough extension | empirical 15-25 min / legacy Small 30-60 min | dual-band | 0:20 | **in empirical band (mid)** (vs low 15: +33%; vs mid 20: 0%; vs high 25: −20%) / **below legacy band low edge** (vs low 30: −33%; vs mid 45: −56%; vs high 60: −67%) | Tenth dual-band datapoint — **first pure-doc task in P1** (no code changes). Scope: extend existing `docs/runbook.md` 306 → 607 lines with new operator-facing sections (Prerequisites, Installation, workspace-block under Configuration, full Operating-delegations P1 section covering all three MCP tools + DelegationReport field semantics + audit-trail note, four new Troubleshooting items per dispatch Decision 3 for WSL pre-flight + undici warning + cloudflared per OS + Node engine matrix, P1 harnesses sub-section, Uninstallation); extend `docs/walkthrough.md` 287 → 476 lines with appended `## P1 — Delegation surface` major section across 11 sub-sections (P1 overview with layered architecture ASCII diagram + job lifecycle Job/JobRunState/JobView split + three MCP tools with audit-metadata side-channel + workspace registry stub + snapshot/diff git-and-fallback paths + transcript writer 50MB cap + orphan handling + report assembler with 4-tier truncation precedence + SDK integration with permission-mode mapping + READ_ONLY_DISALLOWED_TOOLS belt-and-suspenders rationale + bash deny via canUseTool + AbortController cancellation + acceptance harnesses + cross-platform CC-1-through-CC-6 + P2 deferrals); update milestones/project-state/calibration-log tracking entries. **No code changes; lint untouched; 280 daemon tests preserved; 30th consecutive zero-fire on async-discipline rules.** **Reactive at-site decision:** existing `runbook.md` (306 lines, T-0020-produced) was extended rather than replaced — dispatch said "new" but the file already existed with valuable P0 content (lifecycle, MCP-client setup, AC verification procedures). Preserving + extending is more conservative than replacing; new P1 sections inserted at the right structural points (Prerequisites + Installation at top before Lifecycle; workspace block under Configuration; Operating delegations between Files and Troubleshooting; new troubleshooting entries appended to existing Troubleshooting; Uninstallation at end). Cross-references between runbook + walkthrough + design doc + v0.5 methodology threaded throughout (§6 docs-vs-runtime, §7 harness brittleness, §8 CC-N artifacts cited at relevant sections). **Calibration:** **legacy band came in 56% below midpoint** — pure-doc work is faster than v0.4's "Small 30-60 min" bucket predicted; the empirical band's 15-25 min framing is closer to reality for doc tasks. Reasoning consistent with prior pure-doc datapoint at T-0020 (medium-consolidation 6 min vs medium-fresh 60-90 prediction = also a major legacy-band miss). **v0.5 signal:** legacy bands for doc tasks need a "doc-extension" sub-bucket distinct from "design-from-scratch" doc tasks. ~500-line technical-prose extension consolidating existing knowledge runs at typing speed; the empirical 15-25 band is right; legacy Small 30-60 systematically overestimates. **First task operating under methodology v0.5** — dispatch included the new mandatory "User interaction during task (mandatory per v0.5)" section and verdict requires explicit variance arithmetic (computed above). |
